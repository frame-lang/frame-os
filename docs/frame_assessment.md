# Frame in an OS kernel — a running, honest assessment

**Purpose.** A candid, evidence-based evaluation of how well Frame (the state-machine language) serves an *operating-system* codebase, written from inside the Frame OS build. Not marketing: the goal is to record where Frame genuinely earns its place, where it's neutral ceremony, and where its execution model actively fights kernel realities — so the language's value for systems work can be judged from real evidence rather than intuition.

**Method.** One entry per major milestone (B0…), each noting the Frame systems introduced, what Frame bought *over plain Rust*, and what it cost. Plus a cross-cutting "OS-level findings" section that's the real payload. **This is a living document** — appended as the project progresses; the change log at the bottom tracks revisions.

**Standing caveat.** The maintainer explicitly directed "maximize Frame utilization, even for one-state components." So some Frame usage here is *deliberate demonstration*, not the leanest engineering choice — that's called out where relevant rather than scored as a Frame win.

---

## TL;DR verdict (as of B5 Step 4e)

- **Frame is a clear net win for exactly one class of problem: genuinely state-shaped, multi-state protocols/lifecycles where the same event means different things per state.** `TcpConnection` (RFC-793) is the strongest case in the codebase; `PageFaultHandler`'s classify-then-funnel is a solid second.
- **It is roughly a wash for the many small lifecycle/mode machines** (`Mount`, `OpenFile`, `UdpSocket`, `SerialDriver`, `Scheduler`, `BlockRequest`): a `bool`/`enum` would be fewer lines; the Frame version is justified mainly by uniformity + free diagrams.
- **It does not touch the hard ~70% of a kernel** (paging, GDT/IDT, context switch, DMA, checksum/sequence arithmetic), and **every difficult bug in the project lived in native code or the Frame↔native boundary**, not in Frame state logic.
- **Its runtime model is mismatched with kernel constraints** in two concrete, load-bearing ways (per-event heap allocation; run-to-completion on a shared, stateful instance) that forced real indirection (`post`/`drain`, pending-flags) and would be unacceptable on a production hot path.
- **The project's truest value is as a Frame stress test** — it has already produced a fixed framec bug (stringified enter args) and a reusable idiom (timer-via-enter-handler + native wheel) — more than as evidence Frame makes OS development easier.

---

## Per-milestone

### B0 — boot (`Kernel` HSM, `SerialDriver`)
- **Bought:** the `Kernel` boot chain (`$Booting` parent → init-phase children → `$Running`) gives a readable boot sequence, and the `=> $^` panic-forwarding (write the panic path once on the parent, inherit it per phase) is a genuine, if small, win over five hand-copied panic calls. The committed `kernel.svg` is real documentation.
- **Cost / neutral:** `SerialDriver` is a 2-state init gate (`$Uninitialized → $Ready`); a `bool initialized` is equivalent and shorter. The doc admits it's "deliberately minimal." Net: the HSM proved out `=> $^` forwarding end-to-end before anything depended on it — useful as scaffolding, marginal as engineering.
- **OS note:** none of the actual B0 hard work (Limine handoff, GDT/IDT, paging bring-up, UART register programming) is Frame; it's all native. Frame organized the *narration* of boot, not boot.

### B1 — preemptive scheduling (`Scheduler`, `Task`)
- **Bought:** little, honestly. `Scheduler` is `$Idle`/`$Active` (a runnable-count mode); `Task` is a lifecycle that the architecture itself notes omits `$Running` because "on the CPU" flips every timer tick from the ISR, which **cannot fire Frame events** (non-reentrant) — so the most important scheduler state is *deliberately not* in Frame.
- **OS note (important, recurring):** the very first milestone surfaced the core mismatch — the preemptive scheduler's hot transition (which task is running) is native because the ISR can't drive Frame. Frame models the *coarse* mode; native does the real switching. This pattern repeats everywhere.

### B2 — virtual memory (`PageFaultHandler`)
- **Bought (real):** `PageFaultHandler` is the first place `=> $^` becomes load-bearing: `$Classifying → $LazyFault` recovers; on giving up, children self-send `unrecoverable()`, funneled to one parent handler that decides `$Killing` vs `$Fatal`. "What to do when we give up" is written once. In plain Rust this is a helper everyone must remember to call; the Frame version makes it structural. Classify-then-dispatch is a legitimate state-machine shape.
- **Cost:** the fault *data* (CR2 address, error code, is_user) is shared/queried state, not flowing data — correctly kept in the domain/native side. (When we later tried to "thread it" for the pipeline exercise, it didn't fit — see B5.)

### B3 — the multitasking core (`SyscallDispatcher`, `Process`, `ProcessTable`, `ElfLoader`)
- **Bought:** `SyscallDispatcher`'s `$Active.reject` funnel (the `=> $^` showcase again) is clean. `ElfLoader`'s phase pipeline with a single `$Failed` sink (rollback written once) reads well. `Process`/`ProcessTable` are reasonable lifecycle models.
- **Cost (the big one — see OS findings #2):** B3 is where Frame's execution model *created* a bug class. Implementing `exit`/`wait` the obvious way — diverging/blocking *inside* a `SyscallDispatcher` handler — corrupts the **single shared** dispatcher (leaves it stuck in `$Executing`), silently dropping the next process's syscalls (and deadlocking `wait`). The fix is pure Frame-tax: `PENDING_EXIT`/`PENDING_WAIT` flags so the block/diverge happens *after* the handler returns and the machine has settled. A plain `match` syscall demux has no "stuck mid-transition" state to corrupt.
- **OS note:** every genuinely hard B3 bug — register clobbering across the `syscall` boundary, the `IF=0` dead-task park hang, the `task_unready` scheduler race — was in native/asm or the boundary. Frame's state graphs were correct; the difficulty was elsewhere.

### B4 — block device + filesystem (`BlockRequest`, `Mount`, `OpenFile`)
- **Bought:** `OpenFile`'s "access mode is the state" (a write to a read-fd is *undispatchable*) is a nice correctness-by-construction property. The `post`/`drain` pattern is *born* here and is genuinely good design.
- **Cost / neutral:** `BlockRequest` and `Mount` are small lifecycles a `bool`/`enum` covers. The on-disk format, buffer cache, virtio-blk DMA, path walking — all native, all the actual work.
- **OS note (the decisive finding — see #3):** `post`/`drain` exists because **you cannot dispatch a Frame system from an interrupt handler**: Frame dispatch allocates (`Rc` + `BTreeMap`) through a spinlocked heap, and allocating in an ISR that preempted a lock-holding mainline is a hard deadlock. (Reentrancy is a *secondary*, mitigable concern — interrupt masking would fix it; the allocation deadlock is *not* mitigable while Frame allocates per event.) The constraint happened to push us toward the correct top-half/bottom-half architecture — a constraint that coincides with good practice.

### B5 — networking (`ArpResolver`, `RxPipeline`, `UdpSocket`, `TcpConnection`)
- **Bought (the headline win):** `TcpConnection` is the best case for Frame in the whole project. TCP's per-state event interpretation (a FIN means passive-close in `$Established`, simultaneous-close in `$FinWait1`, advance-to-`$TimeWait` in `$FinWait2`) is *exactly* what a state machine is for. The FSM was **correct on the first host run** — 16 behavioral tests mapped 1:1 to RFC-793 transitions, including simultaneous open/close, and the state logic never needed debugging. The forced enumeration of states/events produced a right-first-time control skeleton. The `$Open.rst` funnel makes RST-from-any-state one handler. The committed SVG *is* the RFC-793 diagram.
- **Bought (a genuine idiom):** `ArpResolver` established the answer to "Frame has no `after(ms)`": **arm the timer in the enter handler, cancel in the exit handler, let a native timer wheel fire a `timeout()` event via `post`/`drain`.** `TcpConnection` reuses it for retransmit + `$TimeWait`. This is a clean, reusable pattern and a real positive finding.
- **Cost #1 — the pipeline-threading was partly ceremony.** Converting `SyscallDispatcher`/`ElfLoader`/`RxPipeline` to thread data via enter params is, in straight Rust, "pass an argument." The payoff is "the data flow appears in the diagram." Worth it as a cookbook demonstration (and it surfaced the stringified-enter-arg bug); not obviously the leanest design. `PageFaultHandler` was *correctly left unconverted* because its data is shared/queried, not flowing — an honest case where the pattern doesn't fit.
- **Cost #2 — the runtime allocation mismatch is now load-bearing.** `RxPipeline.deliver()` and `TcpConnection.segment()` allocate `Rc` + `BTreeMap` **per packet/segment**. Fine for a one-connection demo at trivial rates; a non-starter for a real TCP stack (no kernel allocates on the packet path). The headline system, at production scale, would have to abandon Frame's runtime exactly where it matters most.
- **Boundary friction returned:** the live TCP work's hard parts were all native/boundary — slirp accepting host connections before the guest handshakes (forcing connection-recycling + a "wait for `[tcp] listening`" probe), and QEMU `guestfwd` connecting to its target at startup. Frame's FSM was not involved in any of these.

---

## OS-level findings (the real payload)

**1. Frame's leverage over a kernel is bounded by construction (~30/70).** It governs control flow; native owns data, primitives, unsafe, and arithmetic — which is the majority of a kernel and essentially all of its difficulty. So even a *great* result on the Frame portion moves the whole-project needle modestly.

**2. The Frame↔native boundary is where the bugs are.** State graphs came out correct and were cheap to test (host behavioral tests map 1:1 to transitions). Every painful bug was native (asm, DMA, sequence/checksum, slirp/QEMU quirks) or at the boundary (the shared-dispatcher corruption). Frame neither caused most of these nor helped debug them — except the shared-dispatcher class, which it *did* cause.

**3. The runtime model is the core mismatch, and it has two heads:**
   - **Per-event heap allocation.** Every dispatch allocates (`Rc<FrameEvent>` + `BTreeMap` context). **Measured (R2a, 2026-05-22):** ~**6 heap allocations per event dispatch** on the bare-metal allocator — 656 allocs across 112 `TcpConnection` dispatches (16 instances × a 7-event lifecycle), all 16 correct, no OOM on a 256 KiB heap. Consequences: (a) **cannot dispatch from an ISR** — allocating against the spinlocked heap while a preempted mainline holds the lock deadlocks; this forces `post`/`drain`. (b) **fine for control-plane, untenable on hot paths** — ~6 allocs for a connection's whole lifecycle is nothing, but ~6 allocs *per segment* on a data path is unacceptable. Both are the same root cause.
   - **Run-to-completion on a shared, stateful instance.** A handler must return so the generated `__kernel` can finish the compartment switch; the "current state" is mutable data on a single shared object. Consequence: **you cannot block or diverge inside a handler** without leaving the shared machine corrupt — forcing the `PENDING_*` deferral indirection.

**4. Where Frame is unambiguously good:** the `=> $^` parent-funnel (write a disposition once, inherit structurally) and the forced-explicitness discipline (the compiler makes you enumerate each state's handled events, which produced a correct-first-time TCP FSM). These are real, repeatable wins on state-shaped problems.

**5. Where Frame is overhead:** small 2-state lifecycle/mode machines generate ~200 lines of framework (event enum, compartments, dispatch kernel, alloc) to replace a `bool` + an `if`. Uniformity and free diagrams are the only justification; correctness/expressiveness gains are negligible at that size.

**6. Tooling/process wins are underrated:** committed, drift-checked state-graph SVGs and the 1:1 transition→test mapping are genuine workflow benefits independent of the runtime. A reviewer audits the TCP graph without reading code; CI fails on drift.

**7. framec maturity, as exercised here:** found and (maintainer-)fixed the stringified enter/exit-arg bug (typed values were round-tripped through `String`, breaking byte arrays/structs — now typed per-state context structs). A second codegen bug surfaced at B5 Step 6: a state literally named **`$Empty`** collides with framec's synthesized reserved `Empty` variant in the generated per-state context enum (used as its `Default`), producing two `Empty` enum variants → a compile error. It only triggers for that one state name; the synthesized sentinel should be namespaced against user state names. Worked around by renaming the state `$Idle` (the idiomatic name anyway), but it's a real bug to fix. Minor ongoing friction: native statements in handlers need manual semicolons; debugging a misbehaving handler means reading generated Rust. No correctness problems in the generated code itself once a system compiles.

---

## Scorecard

| Dimension | Verdict |
|---|---|
| Complex protocol/lifecycle FSMs (TCP, PFH) | **Net positive** — the reason to use Frame |
| Async hardware-op lifecycles (xHCI: USB enum/transfer) | **Net positive** — the TCP "enter kicks the op, completion event advances" shape generalized to register-level hardware unchanged |
| Error/disposition funneling (`=> $^`) | **Net positive** — small but repeatable |
| Forced-explicitness → correctness | **Net positive** — TCP correct first try |
| Diagrams as committed, checked docs | **Net positive** — workflow, not runtime; highest per-system payoff on register-dense protocols (USB) |
| Driver-loop dispatch by `state()` matching | **Mild negative** — when an FSM reacts to undifferentiated external completion events, the native driver must match on state names to pick the next event (stringly-typed boundary coupling) |
| Small lifecycle/mode machines | **Neutral** — ceremony vs. a `bool`/`enum` |
| Data threading via enter params | **Neutral→negative** — vs. function args; doc value only |
| Interrupt-driven paths | **Negative** — per-event alloc forces `post`/`drain` |
| Cross-core driving of an FSM (SMP) | **Net positive** — the single-owner instance + `post`/`drain` gives cross-core safety for *free* (only `Send` data crosses; no framec `Send`/`Sync` needed). The model's liability becomes the concurrency-correctness mechanism |
| Blocking/diverging control flow | **Negative** — forces `PENDING_*` deferral |
| Performance-sensitive hot paths | **Negative** — per-event allocation |
| The hard 70% (native primitives) | **N/A** — Frame doesn't apply |

---

## Open questions to watch (future milestones)

- **B5 scale:** many concurrent `TcpConnection` instances + a connection table — does the per-event allocation + per-instance dispatch hold up, or does it confirm the hot-path concern quantitatively?
- **B6 (USB):** ~~deep protocol HSMs with timed transitions and many ports — another genuine FSM domain; should be a *positive* case like TCP.~~ **Answered (2026-05-22):** confirmed a *positive* case — the TCP async-lifecycle pattern generalized to xHCI unchanged (see the B6 change-log entry). Caveat: the *many ports / orthogonal regions* sub-question was **not** exercised (single-port demo); it folds into B7's multi-instance-under-concurrency test.
- **B7 (SMP):** ~~the concurrency gate — does framec need `Send`+`Sync` codegen, and does the cross-core `post` story hold?~~ **Partially answered (2026-05-22):** the cross-core `post` story **holds without framec `Send`/`Sync`** — the post/drain architecture keeps the instance single-owner and only `Send` data crosses cores (see the B7 cross-core change-log entry). framec does *not* need `Send`/`Sync` codegen for the pinned-owner model. **Still open:** (a) per-CPU preemptive scheduling — does the *existing* `Scheduler`/`Process` model run cleanly per-core (native work, not a Frame question); (b) would any real design *need* a genuinely shared (multi-core-mutated) FSM instance, which would still hit the gate — so far none has.
- **A no-alloc / preallocated event path** would resolve both heads of finding #3 — worth flagging to framec as the single highest-value change for systems use.

---

## Change log
- **2026-05-21** — initial draft, covering B0–B5 Step 4e. Established the per-milestone format + the OS-level findings (esp. the allocation-driven `post`/`drain` and the run-to-completion `PENDING_*` constraints). To be appended as B5 finishes and B6/B7 begin.
- **2026-05-21** — B5 Step 5 (TAP inbound responders). One small Frame-positive worth recording: adding the inbound ARP/ICMP responders required **no new states and no new events** — they slot into the existing `RxPipeline` `$Arp`/`$Icmp` leaf handlers (the frame is already classified to that leaf; the leaf just gains a "reply" branch). This is the classify-once, dispatch-to-leaf shape paying off: behavior grows at the leaf without touching the graph. The actual responder logic is, as ever, native (Ethernet/ARP/ICMP encode), so Frame's contribution here is purely the dispatch structure, not the protocol work — consistent with the standing ~30/70 split. (No new Frame findings; the headline B5 conclusions stand.)
- **2026-05-22** — B5 Step 6 (IP reassembly, `IpReassembly`). Three observations. (1) **A cleaner enter-param shape than the earlier pipelines:** the *self-transition* `-> (frag) $Reassembling` makes the per-fragment "store + check completion" the state-entry action, re-run with fresh enter-param data each fragment — a collect-until-whole loop expressed as state re-entry rather than a `while`. This is the most natural data-threading use seen so far (vs. the "neutral→negative" verdict on threading generally): the loop's body genuinely *is* a state action, and the timeout escape is a first-class edge. Still, the *value* delivered is modest — a 4-state lifecycle wrapping a native algorithm. (2) **The hard part stayed native, as predicted:** coverage bitmap, hole-free completion check, datagram reconstruction, and outbound fragmentation are all native; Frame owns only "idle/collecting/done/expired." The ~30/70 split holds essentially unchanged. (3) **The two bugs found were both outside Frame:** a virtio-net TX fire-and-forget buffer race (driver) and the framec `$Empty` name collision (codegen, finding #7) — neither a Frame *runtime-model* issue. Net: reassembly is a *mild positive* for Frame (the self-transition shape is genuinely nice) without changing any headline conclusion.
- **2026-05-22** — **B6 (USB / xHCI) complete** — `HubPort`, `UsbEnumeration`, `UsbTransfer` over a native xHCI driver, validated on the real qemu-xhci controller + usb-kbd (controller bring-up → port reset → enumerate to `$Configured` → a real interrupt-IN HID transfer). The standout finding, and the answer to the open question "should B6 be a positive case like TCP": **the B5 networking pattern generalized cleanly to a deep hardware protocol.** All three USB systems reuse the identical shape — *the state's enter handler kicks an asynchronous hardware operation (issue a command / queue a transfer, non-blocking), and the controller's completion event, dequeued by the native driver loop, fires the FSM event that advances to the next stage.* TCP segments, xHCI command completions, and xHCI transfer events are all "an async thing finished" events; the FSM shape is the same. That this carried from a software protocol (TCP) to register-level hardware (xHCI rings/doorbells/contexts) without adaptation is a genuine, repeatable win — the strongest evidence so far that Frame's value is real on *lifecycle-shaped* problems regardless of domain. The `=> $^` funnels also recurred naturally (`HubPort.$Attached` disconnect, `UsbEnumeration.$Enumerating` fail).
  - **A nuance on run-to-completion:** finding #3 calls "can't block in a handler" a *negative* (forces `PENDING_*`). For *inherently asynchronous hardware*, the same constraint is **neutral-to-helpful** — it pushes you toward the correct driver structure (enter handler issues the op and returns; a separate loop reaps completions) rather than the tempting-but-wrong "issue and busy-wait inline." The constraint is a negative only when the *natural* code would block; for hardware that's genuinely async, blocking was never the right design.
  - **The ~30/70 split held, if anything more lopsided.** The native xHCI surface (MMIO mapping, reset choreography, DCBAA/command/event/transfer rings + cycle bits + Link-TRB wrap, slot/EP input contexts, doorbells, TRB encodings) dwarfs the Frame logic — each USB system is a 4–5-state lifecycle wrapper. Frame contributes the *control structure + the diagram*, not the protocol mechanics. For a protocol this register-dense, the committed state-graph SVGs are arguably the highest per-system payoff (a reviewer sees the enumeration/transfer lifecycle without reading 700 lines of `xhci.rs`).
  - **A new small friction (boundary, not runtime):** the native driver dispatches the right milestone event by **matching on `fsm.state().as_str()`** ("Powered"/"SlotEnabled"/…) — because different completions need different next-actions, a single generic "completed" event won't do. That's a *stringly-typed coupling* between the driver loop and the FSM's state names. It works and is localized, but it's the inverse of the usual Frame win (normally the FSM owns the dispatch; here the driver must know the FSM's stage to feed it). Worth noting as the cost of the "FSM reacts to external completion events" pattern when the events themselves are undifferentiated.
  - **Not exercised: orthogonal regions / multi-instance concurrency.** The roadmap flagged "multiple ports concurrently" as a B6 framec gate; the demo is single-port / single-device / single-flight, so that gate is **untested** — it really lands at B7 (many instances under true concurrency). Honest gap.
  - **Challenges were all native or tooling, none Frame-runtime:** the xHCI MMIO BAR sits in QEMU's high PCIe hole that Limine's HHDM doesn't map (needed the kernel's first explicit MMIO mapping); a virtio-net TX fire-and-forget buffer race surfaced when outbound IP fragmentation sent frames back-to-back; the framec `$Empty` codegen collision (now fixed in framec); and the QEMU-monitor `sendkey` needed the socket held open ~100ms so HMP processed the command. Each cost real debugging; none touched the Frame model. Consistent with finding #2 (the bugs live in native code and at the boundary, not in the state machines).
- **2026-05-22** — **B7 (SMP) Step 1: AP startup + per-CPU data — done.** Pure native, no Frame content yet (Limine MP request → `ap_entry` per AP → GS-base per-CPU block → park; `-smp 4`, all 4 cores online). Recorded here only to mark B7 begun and to flag what's coming: **the concurrency model is fine-grained per-CPU from the start** (decided with the user — per-CPU run state + per-structure locks + documented lock ordering, not a Big-Kernel-Lock staging), and the Frame-relevant reckoning is **Step 4's cross-core `post`**. The hypothesis to test there: the post/drain architecture may already give cross-core safety *without* needing framec `Send`/`Sync` codegen — if the Frame instance stays pinned to one drain context and only plain `Send` *event data* crosses cores via an MPSC queue, the non-`Send` instance never moves between cores. If that holds, the long-flagged "framec needs `Send`/`Sync`" gate is sidestepped by the architecture rather than by a codegen change; if something forces sharing the instance, we hit the gate for real (→ a framec feature request). Either outcome is a headline B7 finding. (No scorecard change at Step 1 — nothing Frame-shaped happened.)
- **2026-05-22** — **B7 Step 2: the locking foundation (`SpinLock`) — done.** Still native, no Frame content (an IRQ-safe spinlock + a 4-core counter-hammer proving no lost updates under true parallelism). Recorded because it sets up the Step-4 reckoning concretely: the lock primitive that the cross-core `post` MPSC queue will use now exists and is proven correct under contention. The interesting Frame-adjacent note is the **IRQ-safe** design — disabling interrupts while a lock is held is the *same hazard* that drove B4/B5's `post`/`drain` split (a Frame dispatch from an ISR deadlocks on the spinlocked heap). The spinlock makes that hazard explicit and bounded at the primitive level. It reinforces finding #3's first head (per-event allocation behind a spinlock is the root constraint): now there are *two* reasons an ISR mustn't dispatch a Frame system — the heap spinlock *and*, on SMP, any other lock the mainline might hold — both resolved by the same "ISRs only `post`, the owning core drains" architecture. The next step tests whether that architecture extends cleanly to cross-core posting.
- **2026-05-22** — **B7 cross-core `post` — the headline SMP finding.** The question flagged since the earliest analysis: a Frame system is *non-reentrant, allocating, and a single shared instance* — how does that survive true parallelism? Concretely: framec's generated code is **neither `Send` nor `Sync`** (it uses `Rc`, a single owned instance). The naive fear was that driving a Frame system across cores would require an `Arc`-based / `Send` framec codegen mode — a real language-runtime change. **Result: it does not. The concurrency gate is sidestepped by the post/drain architecture, with zero framec changes.** A Frame system (`EventCounter`) was driven from all 4 cores: the instance is a **local pinned to the owner core** (the BSP), other cores only enqueue plain `Send` event *data* into a `SpinLock` MPSC ring, and the owner drains + dispatches. It compiled (no `Send`/`Sync` bound ever demanded — the instance is never shared) and ran correctly (exact count of 800 = once-only dispatch of every cross-core post; a post-`close()` tick correctly dropped by `$Closed`). **The deeper point — and a genuine reframing of finding #3:** the per-event-allocation / non-reentrant / single-instance model, which is a *negative* for ISR latency and forces the `post`/`drain` indirection, turns out to be exactly what makes Frame **safe under concurrency for free**. You never have to make the state machine thread-safe; you keep it single-owner (which it already is) and marshal `Send` events to it. The `post`/`drain` pattern that was overhead at B4/B5 is the *concurrency-correctness mechanism* at B7. So the model's biggest apparent liability (shared mutable instance, no `Send`) is neutralized by the discipline the model already forces. This is the strongest single vindication of the Frame runtime model for OS work so far. **Caveats (honest):** (1) this is one instance driven by posts, not many instances migrating between cores or a Frame system *shared* (mutated) by multiple cores simultaneously — the pinned-owner model is the safe one, and it's the one the architecture naturally yields, but a design that genuinely needed a *shared* FSM instance would still hit the `Send`/`Sync` gate. (2) per-CPU preemptive scheduling (LAPIC timers, B7-1's "each core runs threads") is still ahead and is pure native work. (3) the cross-core *latency* of marshalling through a locked queue is real, same as the ISR `post`/`drain` cost — fine for control-plane events, not for a hot data path.
- **2026-05-22** — **B7 Step 4: per-CPU LAPIC timer preemption — done (B7-1 met).** Pure native, no Frame content (LAPIC bring-up + a per-core periodic timer + a new naked ISR + AP GDT/IDT load; each AP runs a timer-preempted busy loop). Recorded only to close out caveat (2) above: per-CPU preemptive execution now works (each of 4 cores runs a time-sliced thread). One Frame-adjacent note worth keeping: this is the layer where a *per-CPU run-queue scheduler* would eventually drive the existing `Scheduler`/`Process` Frame systems on each core — and that would be the cross-core-post pattern again (each core owns its scheduler instance; rebalancing/wakeups from another core arrive as posts), so the Step-3 finding (single-owner instance + `post`/`drain` ⇒ cross-core-safe with no framec change) is expected to carry to real per-core scheduling. Not built yet; flagged as the natural continuation. No scorecard change — nothing Frame-shaped happened at this step.
- **2026-05-22** — **B7 Step 5: TLB shootdown — done. B7 (and B0–B7) substantially complete.** Pure native (LAPIC IPI + a shootdown ISR + the cross-core flush barrier). No Frame content; recorded to mark the milestone closed. **Stepping back across the whole project (B0–B7):** the assessment's headline conclusions held up over a full real-OS-class feature set. The repeatable Frame *wins* were consistent — complex protocol/lifecycle FSMs (PFH, TCP, USB enumeration/transfer), the `=> $^` disposition funnels, forced-explicitness producing correct-first-try state machines, drift-checked committed diagrams, and (the B7 surprise) cross-core safety for free from the single-owner + post/drain model. The repeatable *costs* were equally consistent — the ~30/70 native split never moved (Frame owns control flow, native owns every primitive: paging, context switch, DMA rings, MMIO, LAPIC, IPIs), per-event allocation forced post/drain on every interrupt-driven path, run-to-completion forced the `PENDING_*` deferrals, small mode-machines are ceremony, and the ~10× `.frs`→generated-Rust expansion is real. Net: Frame earns its place on the *lifecycle-shaped* parts of an OS and stays out of the way (~70%) of the rest — which is exactly the thesis the project set out to test. The runtime-model liability (non-reentrant, allocating, single shared instance) turned out, at B7, to be the thing that makes it concurrency-safe — the most pleasant surprise of the exercise.
- **2026-05-22** — **R1a: per-core `Scheduler` Frame instances driven cross-core — done.** The first post-B7 refinement, and a direct follow-up on the cross-core-post finding: does it hold with *many* real Frame instances, not just the one `EventCounter`? Yes. Each of the 3 APs owns its own `Scheduler` instance; the BSP posts `task_ready`/`task_unready` into per-core MPSC queues; each core drains into its own Scheduler (`$Idle`→`$Active` peak 3→`$Idle`). Same shape as B7's `EventCounter`, now with N=3 instances of the *actual* B1 `Scheduler` FSM, and results read back via atomics so the BSP never touches an AP's instance. **The prediction from the B7 entry held exactly:** the single-owner + post/drain model scales to N per-core Frame schedulers with **no framec change** — the pinned-owner discipline composes. Caveat unchanged: this is the scheduling *coordination* layer (run-mode per core); per-core context-switched *execution* of multiple threads (R1b) is the deeper native piece and doesn't change the Frame conclusion. No scorecard change — it reconfirms the existing "cross-core driving of an FSM = net positive" row at higher instance count.
- **2026-05-22** — **R2a: per-event allocation finally *quantified*.** The assessment has asserted since B4 that "every Frame dispatch allocates" and called it the root constraint behind `post`/`drain` and the hot-path verdict — but never measured it. Now there's a number: a counting wrapper on the bare-metal allocator + an in-kernel stress (16 `TcpConnection` instances, each driven through a full 7-event server lifecycle = 112 dispatches) reports **656 heap allocations, ≈6 per dispatch**, with all 16 instances transitioning correctly and **no OOM** on the 256 KiB heap. So the cost is concrete: **~6 allocs/event.** Read two ways: (1) *it holds up* — N independent FSM instances at realistic event rates are fine; per-event allocation is a non-issue for control-plane lifecycles (a TCP connection is ~7 events, a USB enumeration ~5, a process lifecycle a handful). (2) *the hot-path verdict stands, now numerically* — ~6 allocations *per TCP segment* (vs per *connection*) on a data path is exactly the kind of thing the no-alloc event path (R4) must remove. This sharpens finding #3 from qualitative to measured and confirms the long-running scorecard verdicts ("hot paths = negative", "small lifecycle machines = neutral") with a concrete figure rather than an assertion. Caveat: this drives the FSMs directly (synthetic segments) to isolate the allocation/instance-scaling question; a live multi-connection TCP server with simultaneous real peers (R2b) would add network realism but not change the allocation number.
- **2026-05-22** — **R2b: N concurrent `TcpConnection` instances over real traffic.** The caveat from R2a is now closed. `tcp.rs` went from a single global connection to a real connection *table* — `Conn` slots, four listening server ports (:7–:10) plus a client slot, inbound segments resolved to a slot by 4-tuple. The key design point for the framec story: the `TcpConnection` FSM was **not changed at all**. A `CURRENT` ambient indirection points the FSM's actions (`send`, `deliver_data`, the arm/drain timers) at the resolved slot for the duration of a dispatch, so each of N instances carries its own seq state and dispatches independently without the generated code knowing the table exists. The harness opens **four simultaneous connections**, echoes on each, and the kernel reports `[tcp] served 4 connections`; the four original B5 single-connection tests still pass after the refactor (**40/40** QEMU). Two things confirmed: (1) the per-instance dispatch model holds at N live connections through the full receive→resolve→dispatch path, not just direct-drive; (2) the allocation number is **unchanged** from R2a, exactly as predicted — multiplexing happens in native table-resolution code, not in the FSM, so it adds no per-event cost. This is the multi-instance-under-real-load complement to R2a's instance-scaling measurement, and it reinforces that the right division of labor is *native owns the table/demux, Frame owns the per-connection lifecycle.*
- **2026-05-22** — **R1b: per-event allocation under N cores scheduling concurrently.** R1a gave each core a `Scheduler` Frame instance but only *tracked* counts via cross-core posts; no core actually time-sliced anything. R1b closes that: each AP now runs a real per-core run queue (`pcsched.rs`) — it spawns kernel-thread workers and time-slices them under its own LAPIC timer, which became a full-frame context-switching ISR (`isr_lapic_timer` → `lapic_schedule` → per-core `pcsched::schedule`, pure native). Spawning/exiting each worker drives that core's own `Scheduler` instance through $Idle→$Active→$Idle, every dispatch in a per-core interrupts-off critical section — the *same* native/Frame discipline as the BSP's single-core `sched.rs`, now replicated N-fold. This is the load case R1a couldn't reach: **3 cores each running a live `Scheduler` instance through real preemptive execution simultaneously, all allocating per dispatch against the one shared, spin-locked heap.** Result: every AP ran all 3 workers to completion with **~42 context switches** and ended `$Idle`; the BSP measured **~57 heap allocs** for the whole phase (3 cores × 6 dispatches ≈ **3.2 allocs/dispatch**). Two findings: (1) **the per-event-allocation model holds under concurrent multi-core scheduling** — no corruption, no deadlock, every instance correct, confirming the B7 pinned-owner verdict (framec needs no `Send`/`Sync`) extends from cross-core *posting* to N cores each *running* their own FSM under preemption. (2) **the per-dispatch alloc cost tracks event complexity** — ~3.2 allocs for the parameterless `Scheduler` events here vs R2a's ~6 for the param-carrying `TcpConnection.segment(seg)`: the gap is the per-parameter Vec/map allocation, which sharpens finding #3 (the no-alloc path R4 must target the *parameter marshalling*, not just the event/context objects). The hard discipline that makes all of this safe is unchanged from B1: the timer ISR is **pure native** (it never dispatches Frame — that would re-enter the allocating, non-reentrant system from interrupt context and deadlock on the heap lock); Frame dispatch happens only in thread context with interrupts off. R1b just proved that discipline composes across cores. **Scope:** kernel threads (ring 0); per-core user processes (ring-3-on-APs + per-CPU TSS.RSP0) are deferred to R5 and aren't needed for the Frame question. Validated by `smp_pcsched_r1b` (**41/41** QEMU).
- **2026-05-22** — **Analysis: is Frame's single-thread-per-instance model a safety advantage?** (Prompted by reflection on the B7/R1 results — recorded because it's the load-bearing argument behind the whole OS architecture.) "Can Frame be multi-threaded?" has two answers because it's two questions. **(1) Many instances, one per thread/core — yes, proven.** R1a/R1b/R2b drove N independent FSM instances across N cores concurrently with zero locks on FSM state and zero races; it's embarrassingly parallel. **(2) One instance mutated concurrently by many threads — no, and deliberately.** framec's generated code is non-`Send`/non-`Sync`; a single instance cannot be driven by multiple threads at once, and you can't recover the capability without destroying the thing — a `Mutex<T>` wrapper serializes every dispatch (lock contention, *no* parallelism, plus reentrancy-deadlock risk if a handler dispatches into itself). The reason isn't a missing feature: **a state machine and shared-memory concurrency are semantically incompatible.** Frame's execution model is run-to-completion (RTC) — an event, including its transition and enter/exit handlers, is fully processed before the next starts — and RTC is precisely what makes "what state am I in?" answerable. Two threads dispatching into one instance produce torn transitions, an exit handler racing the next enter handler, a half-mutated compartment observed mid-flight: the abstraction collapses. This is the actor model (the Erlang backend literally is `gen_statem`: concurrency *between* actors, never *within* one). **Verdict — net safety advantage, with one trap.** It makes an entire bug class *unrepresentable*: data races on state, reentrant-dispatch corruption, torn transitions. Rust's `Send`/`Sync` and Frame's RTC jointly *forbid* them — you cannot even move a non-`Send` instance across a thread boundary without `unsafe`. The recurring "does framec need `Send`/`Sync`?" gate answered itself across B7/R1: **no — because no real systems problem ever needed a shared instance.** Every one decomposed cleanly into per-instance ownership (post/drain for ISRs, single-owner-per-core for SMP, the connection table for TCP). That the constraint never bound in three milestones of systems code is evidence it's the *right* constraint. The deeper, language-design point: RFC-0026's preservation theorem only exists because there's a well-defined operational step (one RTC dispatch) to preserve an invariant across; concurrent intra-instance mutation means there is no atomic "step," so single-ownership is what lets you even *state* soundness. **The trap:** the guarantee only holds if you honor it by message-passing (Send data crosses, instances don't). The moment someone "fixes" the limitation with a `Mutex`, they trade a compile-time guarantee for a runtime lock and get the worst of both. So the honest framing: Frame **trades a capability you almost never want (shared-instance concurrency) for a guarantee you always want (race-free state)**, and the cost only appears if a workload genuinely cannot be sharded — which, so far, has never happened.
- **2026-05-22** — **R3a: the orthogonal-regions question, answered on real hardware.** B6 was single-port/single-device and left open the *orthogonal regions* sub-question: many concurrent lifecycle FSMs of the same type, managed as a group. R3a closes it for USB. `xhci.rs` went from single-flight globals to a per-device table (`Device` slots + a `CUR_DEV` ambient index — the exact `tcp.rs` connection-table pattern), and the *unchanged* `HubPort` / `UsbEnumeration` / `UsbTransfer` FSMs now operate on "the current device." Two HID devices (`usb-kbd` port 5, `usb-mouse` port 6) are enumerated **concurrently**: one `HubPort` + one `UsbEnumeration` instance per device coexist, and a single driver loop demuxes each xHCI completion to the right instance by slot, pointing `CUR_DEV` at it for the dispatch — `slot 1 enabled → addressed → slot 2 enabled → addressed → configured(1) → configured(2) → enumerated 2 of 2`. The significance over R2a/R2b (which also ran N same-type instances): this is driven by **real asynchronous hardware completion events demuxed by an external key (the xHCI slot)**, not synthetic segments — the USB analog of the TCP connection table, on hardware. Same finding, reinforced from a third subsystem: *native owns the table/demux, Frame owns each lifecycle*, and the per-instance model holds for concurrent same-type FSMs regardless of whether the events come from a synthetic harness, a live network peer, or a hardware controller's event ring. One honest seam worth recording: slot assignment **must** be serialized (xHCI's Enable Slot completion carries no port, so you can't correlate a returned slot to a requesting device unless only one is outstanding) — a hardware-imposed serialization point, not a Frame limitation; everything after Address Device carries the slot and interleaves freely. Validated by `usb_multiport_r3a`; the four B6 single-device tests still pass (the keyboard stays device 0 / slot 1, so the B6-4 keypress transfer is unaffected). The B6 → R3a path is the same regression-safe table refactor used for R2b: migrate globals → table with one ambient `CUR_DEV`, prove behavior-identical on the single-instance suite, *then* turn on N.
- **2026-05-22** — **R3b: a new device class + transfer type (USB mass storage / SCSI over bulk).** R3a stayed within one device class (HID); R3b adds a genuinely different one — a `usb-storage` device, bulk endpoints (not the HID interrupt-IN), and a layered protocol (SCSI inside Bulk-Only Transport inside USB bulk). Two things this surfaced are worth recording. **(1) Identity must come from the descriptor, not the table.** Adding a USB3 storage device immediately broke the B6 assumption "device 0 is the keyboard" — being SuperSpeed it sorts onto a lower root-hub port than the USB2 HID devices, so it became device 0 and the keypress transfer started addressing the wrong device. The fix is what a real OS does: read each device's *configuration descriptor* after enumeration, record the interface class/protocol + bulk endpoint addresses, and route by class (keypress → HID keyboard, SCSI → mass storage). This is a native concern entirely; Frame neither helped nor hindered — it's pointer-walking a descriptor blob, the same ~70% of a kernel that's never Frame-shaped. **(2) BOT is a clean Frame win, for the same reason TCP was.** The Bulk-Only Transport transaction is a literal three-phase protocol — Command (CBW out) → Data (in) → Status (CSW in) — where each phase is a separate async bulk transfer whose completion advances the machine. That is exactly the `enter-handler-kicks-the-transfer / completion-event-advances-the-FSM` shape, now with three phases instead of one, and the `fail()`-from-any-phase funnel is the `TcpConnection.$Open` / `=> $^` idiom again. The `UsbMsd` FSM came out clean and was correct on the first QEMU run (INQUIRY, READ CAPACITY(10), and READ(10) of block 0 all returned correct data — vendor "QEMU", 128×512-byte blocks, and the "FRAMEOS!" magic). The pattern holds across yet another protocol: native owns the byte layout (CBW/CSW/CDB) and the rings; Frame owns the protocol phase lifecycle. Net for R3: the orthogonal-regions question is fully answered (R3a, concurrent same-class; R3b, a second class with its own transfer type), and nothing new about Frame's *limits* appeared — the costs are the same per-event-allocation and ISR-can't-dispatch facts already on the scorecard; the wins are the same "multi-state protocol with per-state event meaning" sweet spot. Validated by `usb_msd_r3b` (QEMU) + `usb_msd_behavior` (4 host tests) + a state-graph snapshot.
- **2026-05-22** — **R7a: scheduler-as-actor — the message-passing principle applied *inside* a core.** The multithreading analysis (above) named the rule "post across contexts, call within a context," and flagged one place the OS violated its own discipline: R1a (`ksched.rs`) drove the per-core `Scheduler` through a mailbox, but R1b (`pcsched.rs`) drove the *same FSM* synchronously inside a lock (`with_sched` + `without_interrupts`), because two contexts on the core touched it — a worker (in `exit_current`) and the idle loop. R7a removes that split: a per-core mailbox now carries `Ready`/`Unready` messages; workers and the spawn path only *post*, and the idle loop is the **sole** drainer that dispatches to the `Scheduler` and reads `is_idle()`. One owner ⇒ no lock around the FSM. The `.frs` is unchanged — only *how the instance is driven* changed, from shared-lock to single-owner mailbox. Three things worth recording. **(1) It's behavior-identical** (`smp_pcsched_r1b`: 3 threads sliced, ~40 switches, ended idle, 57 allocs — same numbers as before), confirming this is a pure coupling refactor, not a semantic change. **(2) The one residual critical section is honest.** `exit_current` still wraps the Dead-mark + the mailbox push in `without_interrupts` — but that now guards a *native* handoff (don't let a timer tick strand the worker between marking itself dead and posting its retire), not an FSM dispatch. The FSM itself is lock-free. **(3) This is the native hand-rolling of RFC-0038.** The worker "casts" `task_unready` to the Scheduler's mailbox and the executor (idle loop) delivers it; the missing framec primitive (`@@[cast]` + a drain hook) would replace the mailbox/`SchedMsg`/drain boilerplate with language syntax. R7a is the strongest dogfooding evidence for that RFC so far: it's the *third* hand-rolled instance of post/drain (after ISR→mainline and core→core), now applied to defeat an in-core shared lock — exactly the deferred-dispatch use the RFC argues the runtime spec is missing.
- **2026-05-22** — **R7b/R7c: one mailbox primitive, and a finding about where message-passing actually lives.** R7a left three byte-for-byte-identical hand-rolled FIFO rings (`crosscore::PostQueue`, `ksched::SchedQueue`, `pcsched::SchedMailbox`). R7b extracts one generic `reactor::Mailbox<T, CAP>` (fixed-capacity, no-alloc, `const`-constructible) and routes all three through it — the native realization of RFC-0038's standard mailbox, behavior-preserving (the SMP suite is unchanged). The more useful outcome was R7c's **negative result**, worth recording because it sharpens the whole message-passing thesis: the roadmap had assumed block/net/USB would each get a software mailbox "to match net," but investigation showed **none of them has a software event queue** — net's `RxPipeline`, B4 block, and USB all drain the **hardware** ring directly (virtio used-ring + an `IRQ_PENDING` wakeup; the xHCI event ring via poll). The hardware ring *is* the mailbox. So the OS actually has exactly *two* kinds of post/drain: (a) **hardware→software**, where the queue is a DMA ring the device fills and the driver drains (block, net, USB) — no software mailbox is correct there, and bolting one on would duplicate the ring; and (b) **software→software**, cross-context posts between code paths that can't dispatch directly (ISR→mainline deferred events, core→core, in-core scheduler) — *those* are the three `Mailbox` users, now unified. The lesson for framec's RFC-0038: a language-level deferred-send / `@@[cast]` primitive is the right abstraction for case (b) — the software mailboxes the kernel hand-rolls — but it should **not** try to subsume case (a); device DMA rings are their own (hardware-defined) transport that native code will always own. The cleanest division stays: hardware rings are native; the Frame-level mailbox is for software-to-software deferred dispatch. R7b validated by the SMP suite; no functional change in R7c.
- **2026-05-22** — **R5: deeper SMP correctness — pure native, and that's the point.** R5 added a lock-ordering rank checker on `SpinLock` (panic-on-reversal, with a cross-core nested-lock A→B stress) and per-CPU TSS + double-fault IST stacks (each core `ltr`s its own TSS; #DF → IST1, so a fault lands on a known-good per-core stack instead of triple-faulting). Recorded here only to mark the boundary: **none of this is Frame.** It's register/segment/descriptor and lock mechanics — the same ~70% of a kernel the assessment has said all along Frame doesn't touch. There was no FSM to write, no `.frs`, no Frame finding; the `Scheduler`/`Process`/etc. systems are untouched. This is worth stating positively rather than as an omission: a language for *control-flow state machines* correctly has nothing to say about lock hierarchies or TSS layout, and R5 is a clean example of the division of labor the whole project has mapped — Frame owns protocol/lifecycle state; native owns the machine. (Sleep-locks, the one R5 piece with a plausible Frame angle — a blocking primitive could be modeled as a wait/wake lifecycle — were deferred because they need scheduler integration the current AP model lacks; if revisited they'd be the only part of R5 where a Frame system might earn its place.) Validated by `smp_nested_lock_r5` + `smp_percpu_tss_r5`; the B3 ring-3/syscall path (which depends on the reworked GDT/TSS) still passes.
- **2026-05-22** — **B8: an interactive shell — Frame's sweet spot meets the kernel's hardest bugs.** The first V1.0-track step: a typeable console. The new native plumbing (serial RX + a `read_line` syscall + a console line discipline) is the usual ~70% — no Frame. But the shell itself, `ish`, **reuses the `Parser` FSM** (the same `parser.frs` the hosted shell compiles) to tokenize input in ring 3 — the "one source, two targets" property again, and a REPL is a textbook small state machine. The headline lesson, though, is about the *runtime model under real concurrency*: getting "type a command → a program runs → back to the prompt" working required `fork`, a blocking `read_line`, blocking disk I/O, and `wait()` to all compose — and that exposed a genuine **blocking-I/O bug** (virtio-blk reads busy-`hlt`'d for a completion IRQ instead of yielding; fixed to block the process + wake on the IRQ via `sched::wake_pid`, which is also what lets a *forked* child's disk read complete — it yields the CPU so the IRQ can be delivered and serviced). Two further bugs surfaced only when the shell ran as a lean, demos-skipped *interactive* build, and both were instructive about **latent coupling in the native substrate, not Frame**: (1) the interactive kernel re-ran the entire B0–B7 self-test suite (billion-iteration SMP spin loops) before reaching the prompt — far too slow under TCG — so an interactive build now boots *straight to a shell*, gating the self-tests to the default build; (2) a `#PF` at address 0 on the first context switch into a ring-3 process traced to `gdt::set_rsp0` reading this core's index via `gs:[0]` while the **BSP's per-CPU GS base was still zero** — its initialization (`percpu::init_this_cpu`) had been buried *inside the SMP demo block* the interactive build skips. The fix makes BSP per-CPU init explicit on the interactive path. Both are exactly the standing finding in miniature: **Frame models the lifecycle cleanly (the REPL, the parser), but the kernel's genuinely hard problems — interrupt delivery, per-CPU register state, scheduler/stack discipline — live entirely in the native layer Frame doesn't touch.** Shipped working *and verified end-to-end*: typing `/bin/hello` at the `frameos$` prompt forks, the child reads the ELF **from disk**, execs it, prints, and the shell survives to the next prompt — then `exit` halts cleanly (`cargo xtask console-test` green; default 45-suite unaffected — the feature is gated). This is the disk-loaded-from-shell path that gates B11/B12 (on-device toolchain).
- **2026-05-22** — **B9-1: a growable heap (`brk`) — pure-native mechanism, and honestly so.** The first B9 step toward hosting real toolchains, which need megabytes of heap rather than the fixed 64 KiB static the user programs shipped with. `brk(new_end)` (syscall #10) gives a process a heap it can grow on demand: the kernel demand-maps fresh zeroed `USER|WRITABLE` pages over `[old_break, new_break)` into the process's *own* address space (and unmaps + frees on shrink). The break is per-process state on the `Tcb` (`heap_brk`), seeded at a dedicated VA region (`USER_HEAP_BASE = 0x3000_0000`) chosen to never collide with the program image (`0x1000_0000`) or the user stack (`0x2000_0000`); it composes correctly with the existing process primitives — a `fork`ed child inherits the parent's break (the heap pages ride along in the copied user half), `exec` resets it (new image ⇒ empty heap), and reap's `free_address_space` reclaims the pages with the rest of the user space, so no separate bookkeeping was needed. Recorded here mainly to mark the division of labor again: **there is no Frame system in B9-1, and there shouldn't be.** A program break is a single per-process pointer and a map/unmap loop — address-space mechanism, the same native ~70% as the frame allocator and page tables. There's no lifecycle, no protocol, no per-state event meaning for an FSM to model; inventing one would be ceremony. Frame's place in the V1.0 toolchain track is still ahead and specific (the **build driver** — `framec → mrustc → tcc → link` as a pipeline FSM with a `$Failed` sink). Validated by `brktest` (grows its heap 1 MiB via `brk`, writes a distinct value to every u64 across the new megabyte and reads it all back) + the `brk_growable_heap_b9` smoke test; the default suite is unaffected.
- **2026-05-22** — **B9-2: `argv` through `exec` — the shell finally hands programs their arguments.** `exec_argv` (syscall #11) carries a packed argv (`argc` NUL-terminated strings, `argv[0]` = the program path) from ring 3 into the kernel, which loads the ELF and then writes a **System V x86-64 initial stack** — `argc`, the `argv[]` pointer array, a NULL, an empty `envp` NULL, an `auxv` AT_NULL terminator, and the argument string bytes copied to the top of the page — onto the new program's stack, entering it with `rsp` pointed at `argc`. The shell (`ish`) now packs its parsed tokens into that buffer (so `/bin/argtest alpha beta` delivers three argv entries), and `argtest` reads them back. Two implementation notes worth keeping. **(1) The stack build is trivially correct *because* of an earlier decision.** `exec_image` already switches CR3 to the new address space (`exec_into`) before setting the trap frame — so by the time the kernel lays the argv structure down, the new program's user stack is simply *mapped in the current CR3*, and the kernel writes user VAs directly (ring 0 writing a present, writable user page) rather than juggling the new space's physical frames through the HHDM. The ABI layout (16-aligned `rsp` at `argc`, strings above the pointer block) is the only fiddly part, and it's pure native byte-laying. **(2) The crt0 gap is real and native.** A Rust `extern "C" fn _start` cannot see the initial stack — the args live *at* `rsp`, not in registers — so `argtest`'s `_start` is a three-instruction asm shim (`mov rdi, rsp; and rsp, -16; call argtest_main`) that hands the entry stack pointer to Rust. This is exactly the crt0 a libc provides, and it foreshadows B10: `frame-libc` will own this shim (and `envp`/`auxv` parsing) for C programs. As with B9-1, **there is no Frame system here and shouldn't be** — argv marshalling is calling-convention plumbing, the native ~70%. Frame's place in the toolchain track remains the build *driver* (B11+). Validated by `console-test` (the shell types `/bin/argtest alpha beta`; the program echoes `argv[1]=alpha`, `argv[2]=beta`); the default 46-test suite is unaffected (the existing no-arg `exec`/spawn path is untouched — argv rides a new syscall).
- **2026-05-23** — **B9-3: the file write path — and a small but real Frame win hiding in it.** The fd API was read-only (`open`/`read`/`close`); B9-3 adds what a libc/toolchain needs to *produce* output and inspect inputs: `open` gained a read/write flag (back-compatibly — old callers pass 0 = read), and new syscalls `write` (to a file at the fd's offset), `lseek` (SET/CUR/END), `fstat`/`stat` (size), and `dup` (a shared-offset descriptor). The new on-disk primitive is `fs::write_at` — a random-access write that allocates direct blocks on demand and grows the inode size — which is the write twin of the existing `read_at`; everything else is fd-table bookkeeping (offsets, the writable flag, slot duplication). Mostly this is the native ~70% again. **But there's one genuine Frame contribution worth recording, because it's the first time the `OpenFile` FSM has *earned its keep* rather than just existing:** the access-mode gate. `write()` calls `slot.file.write()` and bails unless the instance `is_writing()`; `read()` does the same for `is_reading()`. So a descriptor opened read-only *cannot* write and vice-versa — not by an `if` the C code has to remember at every call site, but because the `OpenFile` state machine (`$Reading` / `$Writing` / `$Closed`) makes the wrong-mode event a no-op by construction, and a closed fd is inert in the same way. Back at B4 the `OpenFile` system looked like ceremony (one FS, trivial dispatch); B9-3 is where the modeled access mode actually does work, gating a real read/write/dup matrix. It's a modest example, but it's the honest kind: Frame is carrying the *mode-correctness invariant* while native carries the bytes. `getcwd` and `envp` were deferred — both need state (a per-process cwd; an environment) that belongs with `frame-libc` (B10), which is the consumer that will define their shape. Validated by `fwtest` (create `/tmp.txt`, write, seek-and-overwrite the middle via `write_at`, `fstat`, reopen and verify, including a `dup`'d fd that shares the offset) + the `file_write_roundtrip_b9` smoke test; the rest of the suite is unaffected.
- **2026-05-23** — **B9.5: a toolchain-ready filesystem — pure native, and a clean "geometric capacity from linear code" lesson.** B10's first libc-linked program (`cmain`, 8.5 KB) immediately exceeded the FS's **7 KB** file cap (14 direct blocks × 512), and the inode was exactly full at 64 bytes — so the limit was real, not tunable. Rather than a constant fudge, the FS got the proper scalable format: a 128-byte inode with **28 direct + single-indirect + double-indirect** block pointers (max file ~8 MiB), and a **multi-block free-block bitmap** derived from disk size (`Layout::for_total(total_blocks)`, addressing ~2 TB). The kernel maps a file block index through `block_for` (direct → single → double, lazily allocating the index blocks). Worth recording for the *design* lesson, not a Frame one (there's no FSM here — block maps and bitmaps are mechanism): this is the textbook Unix inode, and its virtue is that **each indirect tier is one more bounded `if`/walk while capacity grows geometrically (28 → +128 → +128² blocks)** — linear code, exponential reach. The same is true of the bitmap (one bit per block, indexed by arithmetic) and inode table (more blocks, same `inode_loc` division). The format is shared (`frame_os_shared::fs`) so the kernel driver and host `mkfs` can't drift; the host writes direct-only for now (staged binaries are ≤14 KB; tcc-scale staging teaches the host indirect at B11). The default test disk went 1 MiB → 4 MiB to actually exercise the multi-block bitmap; the *format* scales far past that, but the host `mkfs` builds the whole image in RAM and each smoke test copies it, so the test image stays modest by choice — a deliberate split between *format capability* and *test cost*. Validated by a 128 KiB (double-indirect) write/verify round-trip folded into `fs_file_roundtrip_b4`; the full FS/VFS/shell/persist suite still passes on the new format. The honest header: B9 + B9.5 are ~600 lines of native FS/syscall/address-space mechanism with two small Frame touches (the `OpenFile` mode gate, B9-3) — the toolchain substrate is overwhelmingly the native ~70%, exactly as the scorecard predicts; Frame's turn comes at the build *driver*.
- **2026-05-23** — **B10-1: `frame-libc` begins — a C runtime in Rust, and where Frame will (and won't) live in it.** The first piece of the userspace runtime that hosts real programs: a new `frame-os-libc` crate (`no_std`, `extern "C"` surface), starting with crt0 + syscall thunks + `write`/`exit`/`strlen`. The crt0 is the payoff of an earlier decision — B9-2's `argtest` hand-rolled a three-instruction `_start` shim to read the kernel's SysV initial stack; B10-1 generalizes that into the libc, so *every* program that links `frame-libc` gets a real `_start` (parse `argc`/`argv`/`envp`, call `main`, `exit`) and just writes `main`. The `cmain` test is `#![no_main]` and pulls `_start` from the libc — the exact entry path a tcc-compiled C program will take (crt0 → `main` → libc → syscalls), proven through the shell. The point worth recording is the *plan's honesty* (decided with the user before writing a line): a libc is overwhelmingly native — malloc, `memcpy`, the syscall thunks, crt0 are all mechanism, no lifecycle — so `frame-libc` will have exactly **two** genuine Frame systems and no more: the `FILE*` stream lifecycle (mirroring the `OpenFile` mode gate that earned its keep in B9-3) and the `printf` conversion-spec scanner (a lexer FSM, the same shape as the shell `Parser` reused in B4/B8). Everything else stays native by design; inflating the Frame count by forcing malloc or crt0 into FSMs would be the kind of ceremony the project exists to *avoid* demonstrating. Frame's real turn on the V1.0 track is still ahead and specific — the build *driver* (`framec → mrustc → tcc → link`) at B11+. Validated by `console-test` (`/bin/cmain one two` → the libc crt0 calls `main`, which echoes `argc=3`, `argv[2]=two`).
- **2026-05-23** — **B10-2: `malloc` over `brk` — reuse the free-list, hand-write the C ABI shim.** The libc heap: `malloc`/`free`/`calloc`/`realloc` backed by `linked_list_allocator` (the same crate the kernel + user programs already trust) over a `brk`-grown region. Two things the C ABI needs that a Rust `GlobalAlloc` doesn't, and they're exactly the hand-written part: (1) a **16-byte size header** before each block, because C `free(ptr)` carries no size but the underlying allocator's `deallocate` needs the original `Layout` — so we store the size and reconstruct the layout on free/realloc; and (2) **growth via `brk`** — the heap starts empty, and on a first-fit miss it pushes the program break up (syscall #10) and `extend`s the heap over the freshly mapped pages. The libc is `brk`'s only user in a process, so the region above `USER_HEAP_BASE` is contiguous and wholly owned by the allocator, and since user processes are single-threaded (preemption switches to a *different* process with its own address space) a plain `static mut Heap` needs no lock. This is the chosen-approach (option C) playing out exactly as planned: **reuse the crate for the solved free-list math, write only the ~80 lines of C-ABI glue the reuse can't cover** — no Frame system, because an allocator is mechanism with no lifecycle (modeling an allocation as `Allocated→Freed` would be pure ceremony). Validated by `cmain` allocating 200 KiB (forcing a `brk`-grow past the initial 64 KiB chunk → `extend` → retry), writing + verifying a pattern, `realloc`-growing it with contents preserved, and freeing — asserted through the shell by `console-test`.
- **2026-05-23** — **B10-3a: printf — the first genuine Frame win lands in the libc.** After B10-1/B10-2 were honestly all-native (crt0, malloc), B10-3a is where the predicted Frame content actually appears: `PrintfScan`, a per-character scanner over the C format string, modeled as a Frame FSM (`$Literal`/`$Percent`/`$Width`/`$Done`). It is the **same shape as the shell's `parser.frs`** — the textbook "the meaning of a char depends on the mode" scanner — and this is now the *fourth* compilation of that pattern (hosted shell, `ish`, the kernel, now the libc), the strongest evidence yet for the "one source, N targets" property the project keeps demonstrating. The split is the recurring honest one: **Frame owns the parsing state machine** (which char is a flag vs a width digit vs a literal, by state), **native owns the bytes** (the `printf.rs` engine does number→string for `d/i/u/x/X/c/s/p`, `%%`, and `0`/`-`/width padding). The scanner emits a directive plan (`Vec<PfDir>`, a `Lit`/`Conv` enum held in the FSM's domain — exactly how `parser.frs` accumulates `tokens`); the engine walks it with the argument list. Two implementation notes. (1) **The libc now owns a `#[global_allocator]`** backed by its own `malloc`/`brk`, because the framec-generated scanner code allocates (Rc/Vec/BTreeMap per event) — so a program linking frame-libc gets one heap serving both C `malloc` and Rust `alloc`, which is exactly the relibc model. (2) **Arguments are an explicit `&[Arg]` slice, not C varargs** — a deliberate, user-approved scoping call: true `printf(fmt, ...)` needs the unstable `c_variadic` feature (whole-project nightly) or a hand-rolled SysV `va_list`, and we deferred that thin ABI shim to B11 where its only consumer (tcc) forces the nightly-vs-handroll decision; the scanner FSM + conversion engine it will call are done and proven now. A side effect worth recording: the printf-laden `cmain` crossed 14 KiB (33 blocks), so this step also pulled **host indirect-block staging** forward from B11 — `mkfs` now mirrors the kernel's `block_for` (single + double indirect), closing the last B9.5 deferral and letting the host stage files up to ~8 MiB. Validated by `console-test`: `/bin/cmain` prints `d=-42 u=42 x=ff X=FF c=Q s=world p=0xdead pad=[    7][7    ][00007] pct=%` — every conversion + flag, formatted by native code off a Frame-parsed plan.
- **2026-05-23** — **B10-3b: buffered `FILE*` — the second libc Frame system arrives by *reuse*, and a clean lesson about which dimension to model.** The libc's `FILE*` layer (fopen/fwrite/fread/fputs/fputc/fflush/fclose, stdout/stderr, fprintf) is mostly native buffering over the B9-3 fd syscalls, but its mode gate is a Frame system — and the notable choice is that it's the **kernel's own `OpenFile` FSM, reused**: `libc/build.rs` compiles `frame/open_file.frs` (the *same* file the VFS uses) so `fwrite` is a no-op unless the stream is `$Writing` and `fread` unless `$Reading`, by construction. That makes it the project's first **lifecycle** FSM compiled to two targets (the parser was the scanner precedent; this is the same "one source, two targets" property for a stateful resource lifecycle). The design lesson worth recording: a C `FILE*` has *two* dimensions — a **static** read/write mode and a **dynamic** ok/eof/error status — and they want different tools. The mode is fixed at open and gates operations: a lifecycle, modeled in Frame (the reused `OpenFile`). The eof/error status is two sticky booleans reset by `clearerr`: I deliberately left it **native**, because forcing it into the FSM hits the orthogonal-regions wall (mode × status = a 6-state cross-product, or an `$Error` state that has to remember which mode to return to). So Frame owns the dimension with real transitions and native owns the two flags — the honest division, not "model everything as states." The non-variadic stdio entry points ship as genuine `extern "C"` (a tcc-compiled C program can call `fopen`/`fwrite`/`fread` directly); only the variadic `fprintf(f, fmt, ...)` waits for B11 with the rest of the varargs ABI. Validated by `console-test`: `/bin/cmain` fprintf's to the console, writes `/gen.txt` via fprintf+fputs, reopens it for reading, reads the exact bytes back, and confirms `feof` fires past the end — the full write→close→reopen→read→eof round-trip through the on-disk filesystem, with the reused FSM enforcing the mode at each step.
- **2026-05-23** — **B10-4: line input — pure-native read-side polish, completing the C-side libc.** `fgetc`/`fgets`/`getchar` + an input buffer (`ibuf`/`ipos`) the read path drains, refilling from the file `read` syscall or — for `stdin`, fd 0, the console, which has no plain readable fd — the blocking `read_line` syscall (#9). No Frame here, correctly: stream buffering and line splitting are byte mechanics, not a lifecycle. Recorded mainly to mark a milestone: with B10-1…B10-4 the **C-side libc core is functionally complete for a C hello-world** — crt0, malloc/free over brk, printf (scanner FSM + native conversions), and full buffered `FILE*` read/write (fopen/fwrite/fread/fputs/fgets/fclose, stdout/stderr/stdin). Tallying the Frame content honestly: of the whole libc, exactly **two** Frame systems earned their place — the printf format scanner (new) and the reused `OpenFile` mode gate — and everything else (crt0, allocator, syscall thunks, stdio buffering, conversions, line splitting) is native, as predicted before a line was written. That ratio *is* the finding: a systems library is overwhelmingly mechanism, and Frame's value in it is precise and small (the two genuinely state-machine-shaped pieces), with its real V1.0-track turn still ahead at the build *driver* (B11+). Validated by `console-test`: `/bin/cmain` reads `/gen.txt` back a line at a time with `fgets`, getting `result=42\n`, `second line\n`, then NULL at EOF.
- **2026-05-23** — **B11-1: C-variadic printf — the deferred ABI shim, on stable, and a small honest piece of asm.** A C program's `printf("hello %d\n", n)` is a variadic call; frame-libc's `printf` must read the args off the SysV register/stack ABI. At B10-3 we deferred this with the user; the call (made with the user at B11) was to stay stable and hand-roll the va_list rather than move the whole project to nightly `c_variadic`. The implementation is a `#[unsafe(naked)]` trampoline (naked functions are stable as of 1.88) that spills the vararg integer registers to a stack save area and calls a Rust impl walking (saved regs, then stack overflow) — feeding the *already-built* scanner FSM + conversion engine. Two things made it small rather than the brittle thing I feared: (1) **no `%f`**, so the SSE registers are never touched — the va_list is integer/pointer only, and the gnarly gp/fp-offset cross-product of the full SysV va_list collapses to a single index cursor; and (2) the **alignment falls out of the ABI** — five 8-byte pushes from a post-`call` rsp (≡8 mod 16) land exactly 16-aligned, so `printf` needs no manual stack adjustment (fprintf, consuming rdi+rsi for stream+fmt, pushes four and pads once). A useful stable-Rust fact surfaced: *defining* a variadic `extern "C"` fn needs nightly, but *calling* one is stable — so `cmain` declares `extern "C" { fn printf(*const u8, ...); }` and exercises the real C ABI (the Rust compiler emits the variadic call exactly as a C compiler would), through frame-libc's naked symbol. No Frame here: this is calling-convention plumbing, the native ~70%. With it, frame-libc's printf is genuinely what a tcc-compiled program emits — the gate to B11-2 (a real C program on Frame OS). Validated by `console-test`: `printf("d=%d x=%x s=%s c=%c", -7, 0xbeef, "hi", 'Z')` → `d=-7 x=beef s=hi c=Z`, and the `fprintf` path likewise.
- **2026-05-23** — **B11-2: a gcc-compiled C program runs on Frame OS — and the latent scheduler bug it flushed out.** The milestone itself is the V1.0 half-proof: `csrc/hello.c`, plain C with no Frame-OS-specific code, cross-compiled with `x86_64-linux-gnu-gcc` and linked against `frame-libc` (crt0 + malloc + printf + `FILE*`), baked at `/bin/chello`, runs from the `frameos$` prompt — printf, malloc/free, and a `fopen`/`fprintf` → `fopen`/`fgets` round-trip, all green. No Frame content in B11-2 proper (it's toolchain plumbing — the native ~70%); the Frame pieces it exercises are the already-built printf scanner + reused `OpenFile` FSM. **The entry worth recording is the bug.** B11-2 first shipped the C ELF *unstripped* (~860 KiB) because `--strip-all` *appeared* to make the program fault in-kernel — a documented "anomaly." That diagnosis was wrong, and chasing it down was the most instructive debugging of the V1.0 track so far.
  - **The symptom was a heisenbug**, not a strip problem: the *unstripped* build also failed ~60% of the time under single-core stress (`-smp 1`). So `--strip-all` was a **red herring** — it only changed the ELF's file size, which shifted how much `exec` reads off disk, which shifted **timer-preemption timing**, which exposed a latent race. Ruled out, with evidence, in turn: disk corruption (the on-disk programs were byte-identical, same inode/blocks/SHA in passing and failing images), frame use-after-free (disabling reap-time teardown didn't help; with no frees there was no aliasing), FPU/SSE not saved across context switch (the C program and kernel contain *zero* XMM instructions), and SMP (it reproduced at `-smp 1`).
  - **Root cause — `exec` was the one blocking syscall that broke the run-to-completion discipline.** Targeted instrumentation (snapshot every process's saved frame on switch-out, flag any that changed off-CPU) caught it precisely: the *parent shell's* saved register frame was being overwritten, off-CPU, while a child ran — and a frame dump showed the overwrite was the **new program's exec frame** (`rip` = the child's entry, `rsp` = a fresh user stack, all GPRs zeroed). The mechanism: `CURRENT_TRAP_FRAME` is a single global, justified by "syscalls run IF=0, single-flight, no preemption." But `exec` reads the ELF off disk — a **blocking virtio read (B4) that re-enables interrupts and yields** — *inside* the dispatcher handler. While the child's `exec` was blocked mid-read, the timer ran the parent shell, whose `wait()` syscall overwrote the global `CURRENT_TRAP_FRAME` to point at *its own* frame; the child then resumed and `exec_image_args` did `*CURRENT_TRAP_FRAME = TrapFrame{…}`, installing the new image's registers into the **shell's** saved frame. The shell later resumed in ring 3 with a zeroed callee-saved register and faulted on a null write. (Two bugs, one trigger: the blocking read inside the handler also re-entered the *non-reentrant* `SyscallDispatcher` FSM, since it was parked in `$Executing` rather than back in `$Validating`.) Latent since the blocking-exec + fork/wait shell combo (B4 + B8) — entirely predating B11; B11's bigger program just widened the timing window enough to make it frequent.
  - **The fix is the kernel's own established pattern.** `wait`, `read_line`, and `exit` were already deliberately *deferred* out of the dispatcher handler (the `PENDING_*` flags) precisely so the FSM returns to `$Validating` before blocking, and they operate on the **local** frame pointer, never the global. `exec` was the lone holdout. So `exec` now follows suit: the handler only records `PENDING_EXEC` + args; `syscall_dispatch` performs the (blocking) load + frame install *after* `d.request` returns, on the caller's own `frame` pointer threaded down through `exec_image`/`exec_image_args` — never the global. One coherent change fixes both the trap-frame clobber and the FSM re-entrancy.
  - **The Frame-relevant lesson (a direct reinforcement of finding #3).** The run-to-completion model — "you cannot block inside a handler" — is the assessment's standing *negative* (it forces the `PENDING_*` deferral indirection). This bug is the cost of *violating* that discipline made concrete: `exec` blocked inside the handler and corrupted shared single-flight state across the yield. The `PENDING_*` deferral isn't ceremony; it's the **correctness mechanism** for "a syscall that must block must first let the non-reentrant dispatcher settle and must not hold a global across the yield." The same shape (defer the blocking work, operate on per-context state) that B7/R1 found makes Frame *concurrency-safe for free* is what makes the *syscall layer* safe under preemption — and `exec` simply hadn't been brought into line. Honest scope: the audit also flagged `ELF_BUF`/`ARGV_BUF` as latent single-flight hazards (safe only because the shell runs one `exec` at a time) — noted as a follow-up, not a current bug. Validated: **12/12** `console-test` at `-smp 1` (the stress config that failed 6/10 before the fix), and `console-test` + **47/47** `qemu-test` at the default `-smp 4`. With the root cause fixed, `--strip-all` was re-enabled (the lean ~41 KiB ELF runs correctly) and the misleading NOTE removed.
- **2026-05-23** — **B11-3a: FPU/SSE state across context switches — the foundation for an on-device C toolchain.** First step of B11-3 (run tcc on Frame OS). Until now nothing in the kernel or userspace touched the x87/SSE registers — the kernel is integer-only, and the hand-written user programs + the `-ffreestanding` gcc C programs (cmain, chello) emit *zero* XMM. The on-device C compiler (tcc) and the code it generates use SSE for `double` and x87 for `long double`, so two things became necessary, both pure-native register mechanics (no Frame content — this is squarely the ~70%): **(1)** enable SSE + `fninit` per core (`fpu::init_this_cpu` on the BSP both builds + each AP; Limine already enables SSE per its boot protocol, so the CR0/CR4 writes are belt-and-suspenders + the `fninit` puts x87/MXCSR in a known state); **(2)** save/restore the FPU register file across context switches. The scheduler previously saved only the 15 GPRs + the iretq frame; the XMM/x87 file was *shared*, so two preemptively-interleaved FPU users would clobber each other — the identical bug class as a missing GPR save, just for the FPU. The fix: a per-thread 512-byte 16-aligned FXSAVE area (`FPU_AREAS[slot]`), eager `fxsave` on switch-out + `fxrstor` on switch-in in `sched::schedule`, seeded with a clean (`fninit`) template on every spawn path, `fork` inheriting the parent's live FPU (FXSAVE straight into the child's area), and `exec` resetting to clean (so the old image's MXCSR can't leak into the new program). Eager (not lazy CR0.TS + #NM) — simpler, correct, no lazy-FP-restore leak. **This is a direct continuation of the same session's exec/trap-frame race fix:** that one found the GPR save/restore was being *corrupted*; this one finds the FPU save/restore was *entirely absent* — both are "preserve the full register context across a switch," the most safety-critical invariant in the kernel. Validated with `fputest`, which `fork`s two processes that each pin distinct sentinels into xmm0..xmm7 and verify they survive thousands of preemption windows — and, crucially, with a **negative control**: disabling the new save/restore makes `fputest` FAIL ("parent FAIL: xmm clobbered"), so the test genuinely detects FPU corruption rather than passing vacuously. With the fix: `fputest` PASS and the full suite **48/48** across four consecutive runs (`fpu_preempt_b11` added). No scorecard change — like the GPR context switch, FPU state is register mechanics Frame has nothing to say about; recorded to mark the toolchain foundation laid and the honest native/Frame line held (Frame's turn on this track is still the build *driver*, B11-3e).
- **2026-05-23** — **exec scratch buffers made concurrency-safe — closing the `ELF_BUF`/`ARGV_BUF` follow-up.** The B11-2 trap-frame fix explicitly flagged two siblings as latent single-flight hazards ("safe only because the shell runs one `exec` at a time — noted as a follow-up, not a current bug"): `ELF_BUF` (a 64 KiB static the exec'd ELF is read into off disk, then handed to the loader as a `'static` slice) and `ARGV_BUF` (a 1 KiB static the packed argv is copied into before it's laid onto the new program's stack). Both are the **exact same bug class** as the trap-frame race and the absent-FPU-save: shared state held across `exec`'s **blocking virtio read**, which re-enables interrupts and yields. The window: process A reads its ELF into `ELF_BUF` and blocks mid-read; the scheduler runs process B, whose own `exec` overwrites `ELF_BUF` with *its* program; A resumes and its loader maps **B's image** (and, identically, B's argv). Safe today only because `ish` forks one child and `wait()`s — never two concurrent execs — but a shell with background jobs, or any two forked children both `exec`'ing, would trip it. **Approach — per-exec heap buffers, not a serialization lock.** A lock around the exec critical section would work but is exactly the pattern this kernel deliberately avoids: the trap-frame fix didn't lock `CURRENT_TRAP_FRAME` either, it *deferred the blocking work and operated on per-context state* (the caller's own frame pointer). The same instinct here: each in-flight `exec` allocates its **own** file-sized buffer on the kernel heap (`read_exec_elf` — a fallible `try_reserve`, so a file too big for the heap fails the exec cleanly rather than aborting the kernel via the alloc-error handler), reads into it, hands it to the loader, and frees it after the load; the argv copy is likewise a per-exec heap `Vec`. Two concurrent execs now own disjoint buffers, so neither can clobber the other — per-process state, the codebase's standing preference over a big lock. One subtlety worth recording: the `elf.rs` `ELF` global (which holds the in-flight `bytes` slice + the rollback list) is *not* a hazard and didn't need touching — the ELF *load* itself (`prepare` → `ElfLoader::__create` → segment mapping) is pure in-memory work that never blocks, so it runs atomically under IF=0; only the **disk read** preceding it yields, and that now fills a private buffer. Freeing right after the load is sound for the same reason: no other exec can be reading into *this* buffer between the read returning and the loader consuming it. **Test.** A new `coexec` program forks two children that `exec_argv` *different* programs from disk *at the same time* — child A → `/bin/hello`, child B → `/bin/argtest "Z"` — so their blocking reads interleave; the `concurrent_exec_buffers` smoke test asserts `argtest`'s `argv[1]=Z` appears, which is only possible if child B loaded argtest *and* its argv survived child A's concurrent exec uncorrupted (a clobber or a program/argv swap would lose it), plus both children reap with the correct, *un-swapped* exit codes (42 and 0) and no process faults. No Frame content — this is the **third** instance this session of the same native invariant ("preserve per-context state across a switch or a yield": the GPR trap frame was *corrupted*, the FPU file was *absent*, the exec scratch was *shared*), reinforcing that the kernel's safety-critical context-preservation lives entirely in the native ~70%. Validated: `concurrent_exec_buffers` green and the full suite **49/49** (the new test added); the shared-static exec path is gone.
- **2026-05-24** — **B11-3d (checkpoint): the on-device C compiler runs in ring 3.** After B11-3c finished frame-libc's surface (`tcc` links with zero undefined symbols on the host), this step gets the ~1.2 MiB `tcc` binary itself *running on Frame OS*: `build_tcc_disk_elf` (xtask) cross-compiles the two vendored translation units (`libtcc.c` `-DONE_SOURCE=1` + `tcc.c` `-DONE_SOURCE=0`) against frame-libc and links `/bin/tcc`, staged onto the disk image. Typing `/bin/tcc -v` at the shell prints `tcc version 0.9.27` — proof that tcc's crt0, libc startup, and argv handling all work in user mode. Getting there surfaced **three resource limits that were fine for hand-written programs but not for a real toolchain**, each fixed at the root (no workarounds): **(1)** the **user stack** was a single 4 KiB page — tcc's recursive-descent parser blows that — so the loader (`elf.rs`) now maps a 32-page (128 KiB) stack; **(2)** the **kernel heap** was a fixed 256 KiB BSS static, but `exec` reads each program's image into a per-exec heap buffer (the B11 concurrency fix above) and tcc's 1.2 MiB image overflowed it, so the `try_reserve` failed and the shell reported a bare "command not found" — the heap is now 8 MiB (still BSS, zero on-disk cost; QEMU has >100 MiB RAM); **(3)** the loader's failed-load rollback list (`MAX_TRACKED`) was 64 pages, far short of tcc's ~104 PT_LOAD pages, so a *partial* load would leak — bumped to 512 (the *success* path was never affected: `paging::free_address_space` walks the page tables on exit/reap and frees everything regardless of the tracked list). No Frame content yet — this is the toolchain *substrate* (the build *driver* that orchestrates compile→link→run is Frame's turn, still B11-3e). The remaining B11-3d work (a full on-disk sysroot — `/usr/include` headers, crt objects, `libc.a`, and tcc's own `libtcc1.a` — so `tcc /hello.c -o /out.elf` compiles + links a runnable program) is in progress. Validated: `console-test` runs hello/argtest/cmain/chello + `tcc -v` end-to-end; the larger stack/heap didn't regress any existing program.
- **2026-05-24** — **B11-3d: on-device tcc *compiles + links* — and a deep dive into why its static linker can't eat Rust objects.** Built the on-disk sysroot (multi-level FS paths `/usr/include`, `/usr/lib`; crt1/crti/crtn objects; a partial-linked `libc.a`; vendored tcc intrinsic headers) so `tcc -B/usr/lib/tcc -static /hello.c -o /out.elf` runs on-device. It **compiles and links successfully** — tcc opens every input (source, `<stdio.h>`, `<stdarg.h>`, crt objects, `libc.a`) and writes a valid ELF. But the linked program *crashed at runtime*, and chasing that crash (by extracting `/out.elf` from the disk image with `scripts/extract_fsfile.py` and disassembling it with host `objdump`) root-caused **two real bugs in tcc 0.9.27's fully-static x86-64 linker**:
  - **The PLT is broken.** tcc builds PLT stubs for default-visibility global calls even in a static link (where no PLT is needed — every address is known). Its static-exe PLT stubs have a *wrong* GOT displacement (`jmp *0x30(%rip)` points back *into the PLT* instead of at the far-away `.got`), and the GOT slots are unfilled — so the first cross-object call (`_start → __libc_start`, then `__libc_start → main`) jumps into garbage.
  - **The GOT is unfilled.** Even with the PLT sidestepped (by hiding the libc symbols so tcc resolves `PLT32` directly), the `.got` for the 2547 `R_X86_64_GOTPCREL` data relocations the Rust `libc.a` carries is left all-zero, so the first GOT-indirected access reads a null pointer.
  These are exercised because our `libc.a` is **Rust/LLVM-compiled** (`core` + `compiler_builtins` + frame-libc), which emits pervasive GOT/PLT relocations; tcc's rarely-tested static path mishandles them. Five genuine **Frame OS** bugs were found and fixed along the way (all root-caused, no workarounds): a kernel panic on a 0-length `read`/`write` (`usermode.rs` formed a slice from a NULL user pointer — tcc issues `read(fd,NULL,0)`); a **scheduler TCB-slot leak** (`sched.rs` `spawn_*` appended a slot per `fork` and never reused freed ones, so the 7th sequential program wrote `TCBS[8]` out of bounds and corrupted the kernel — now `alloc_slot` reuses reaped slots); the **ELF loader assumed page-aligned segment vaddrs** (`elf.rs` placed file bytes at the page base, misplacing tcc's mid-page `.data` by its sub-page offset — now it honors `p_vaddr & 0xfff`); and the FPU/heap/stack/`MAX_TRACKED` sizing from the checkpoint above. The #PF handler now also prints the faulting RIP (`vm.rs`/`interrupts.rs`), a genuinely useful kernel diagnostic. **Decision (recorded): stop patching tcc; give it a C-shim libc.** tcc itself is the right tool — it's a complete compiler+assembler+linker in one small binary (the alternatives, chibicc/cproc/QBE, only emit assembly and need a separate assembler+linker we'd also have to port). The worm farm is narrowly that tcc's static linker chokes on *Rust/LLVM* objects' GOT/PLT relocations. A small C libc compiled `gcc -fno-pic` emits **zero GOT** relocations (direct data addressing) and, compiled `-fvisibility=hidden`, makes tcc's *existing* linker resolve all `PLT32` calls directly (no PLT) — sidestepping **both** bugs without modifying tcc, so the vendored copy stays pristine for clean re-vendoring. The Rust `frame-libc` remains the runtime for the OS's own programs; the C-shim is specifically the library tcc-compiled C programs link against (a C runtime for a C compiler is the natural arrangement anyway). The tcc bugs are recorded here for a possible upstream report (against tcc's active `mob` branch, not the frozen 0.9.27).
  - **Harness note (important):** while validating, the full `qemu-test` suite reported all tests failing with *empty* serial captures. This is **not** a kernel regression — running the exact non-interactive kernel ELF directly in QEMU (with both `-serial stdio` and `-serial file:`) boots cleanly and prints the entire SMP/B-track demo sequence, exiting via `isa-debug-exit`. The empty captures reproduce at the prior committed checkpoint too (whose changes don't touch the boot path), so it's a harness/environment control-flow artifact (consistent with the documented docker/QEMU statefulness), tracked separately. Kernel validation for this work came from `console-test` (interactive) + direct manual QEMU boots.
- **2026-05-24** — **B11-3d DONE: the OS compiles a C program with its own tcc and runs it — the C half of the V1.0 north star.** The C-shim libc landed (`libc/cshim/cshim.c`, ~250 lines): crt0's Rust half (`__libc_start`), syscall wrappers, `printf`/`puts`/`putchar`, `malloc`/`free`/`calloc` (a bump allocator over `brk`), and `memcpy`/`memset`/`memmove`/`strlen`/`strcmp`. Compiled `gcc -fno-pic -fvisibility=hidden`, it carries **only** `PC32`/`64` relocations — no GOT (direct addressing), and tcc's existing linker resolves every `PLT32` call to a hidden symbol directly (no PLT). That sidesteps **both** tcc static-link bugs from the prior entry with **zero changes to tcc** (it stays pristine for clean re-vendoring). `build_tcc_sysroot` now stages this `libc.a` instead of the partial-linked Rust archive. At the shell: `tcc -B/usr/lib/tcc -static /hello.c -o /out.elf` then `/out.elf` → prints `hello from a tcc-compiled program on Frame OS!` and exits 7 (main's return value). One fix during bring-up: the shim's `write` must route fd 1/2 to the kernel's `write_char` (syscall 0), not the file-write (syscall 12) — same as frame-libc. **The on-disk C toolchain works end to end.** Why a C libc and not Rust's: tcc is a complete compiler+assembler+linker in one binary (the right tool — chibicc/cproc/QBE only emit assembly and would need a separate assembler+linker ported too), but its rarely-used fully-static path can't link Rust/LLVM objects' GOT/PLT relocations; a C runtime for a C compiler is the natural arrangement, and frame-libc (Rust) remains the runtime for the OS's *own* programs. No Frame content yet — the build *driver* (compile→link→run orchestration as an FSM, with a `$Failed` sink) is Frame's turn, B11-3e. Validated: `console-test` runs hello/argtest/cmain/chello + `tcc -v` + the full on-device `tcc /hello.c → /out.elf → exec` end to end; the disk shrank back to 8 MiB (the 7 MiB Rust `libc.a` is gone).
- **2026-05-24** — **Evaluated (and rejected) adopting tcc's `mob` branch instead of the C-shim.** A background research agent confirmed both static-link bugs are *fixed* in tcc's active `mob` branch (0.9.28rc) — the fix is the same `build_got_entries` condition I'd identified, extended with `|| s1->output_type & TCC_OUTPUT_EXE` (in mob ~4 years). The natural question: re-vendor mob, drop the C-shim, and link the real Rust `frame-libc` directly. So I **measured the adoption cost** by actually re-vendoring mob at a pinned SHA (`3b1fe97a`, 2026-05-23) and trying to build it. Verdict: **net-negative, reverted.** mob's newer, larger feature set drags in host dependencies our freestanding cross-build must satisfy, in sequence: generate `tccdefs_.h` (`CONFIG_TCC_PREDEFS`), vendor `dwarf.h` (the new `tccdbg.c` DWARF support), `-DCONFIG_TCC_SEMLOCK=0` (new thread-locking), and — the dealbreaker — stub `<signal.h>` + the **glibc `ucontext_t` layout** (`uc_mcontext.gregs[REG_RIP]`) + export `environ`, all to compile the **`-run` JIT path we never use** (Frame OS writes ELF to disk), which `TCC_IS_NATIVE` force-enables (host arch) and mob's `CONFIG_TCCBOOT` no longer prunes. That's fake host structs carried for dead code, **re-incurred on every mob bump** (mob is unreleased, no QA). 0.9.27, by contrast, built cleanly with just `CONFIG_TCCBOOT`. And the payoff — linking the Rust `frame-libc` — means tcc reads + scans a **7–11 MiB** archive (bundled `core`+`compiler_builtins`) on *every* on-device compile, vs the few-KB C-shim. So the C-shim wins on simplicity, size, *and* on-device link speed; the "use the upstream fix" instinct is right in general, but the upstream that has it (mob) costs more than it saves here. Recorded as a deliberate, data-driven decision rather than a default. Revisit only if we need a feature mob adds. (Pristine 0.9.27 + C-shim restored; tree unchanged from the B11-3d commits.)
- **2026-05-24** — **`qemu-test` "all-49-empty" was environmental flakiness, not a regression — resolved, 49/49 green.** During B11-3d I saw the full `qemu-test` suite fail with *empty* serial captures and spent real effort proving it wasn't the kernel (the non-interactive kernel boots + runs every demo + exits cleanly in ~16 s when launched directly, with both `-serial stdio` and `-serial file:`). Returning to it: after a clean rebuild, `boot_prints_banner` passes through the harness and the **full suite is 49/49**. So the empty captures were a transient — almost certainly artifact thrash from rapidly flipping the kernel's `interactive` feature between `console-test` (interactive) and `qemu-test` (non-interactive) in the same `/target` volume, which forces full kernel + ESP rebuilds back-to-back; combined with the documented docker/QEMU statefulness, a stale/half-written ESP or a cold-boot QEMU hiccup produced the empty serial files. **Lesson for future debugging:** an empty (0-byte) capture across *all* tests is the signature of a build/ESP/QEMU-launch problem, not a kernel bug — confirm by booting `/target/limine-esp.img` directly before suspecting code, and **mind which kernel is in the ESP** (console-test leaves the *interactive* one, which correctly sits at `frameos$` waiting for input and looks like a hang under `-serial file:`). No harness or kernel fix was needed.
- **2026-05-24** — **B11-3e: the BuildDriver Frame FSM — Frame's turn on the toolchain track.** B11-3a–d were all native (FPU save, ELF loader, frame-libc, the C-shim) — the honest ~70%. B11-3e is the Frame content: the build *lifecycle* modeled as a state machine. `frame/builddriver.frs` is a 6-state pipeline — `$Idle →(start)→ $Compiling → $Linking → $Running → $Done`, with fallible phases funneled to a `$Failed` sink that records which phase broke — generated to Rust by framec and driven by a ring-3 program (`user/src/buildc.rs`, staged at `/bin/buildc`). The "Frame owns lifecycle, native owns mechanism" split is exact: the FSM sequences phases and branches on results; the native `crate::actions::{compile,link,run}` fork a child that execs `/bin/tcc` (`-c -B/usr/lib/tcc /hello.c -o /hello.o`, then `-static /hello.o -o /out.elf`) or `/out.elf`, and wait for the exit status — the same shape as the kernel's `ElfLoader` calling `crate::elf::*`. Running `/bin/buildc` at the shell drives the whole pipeline: it compiles + links `/hello.c` via two on-device tcc invocations, runs the result (which prints its message and exits 7), and reports `[build] pipeline ok; /out.elf exited with code 7`. This also proved tcc's **separate compile (`-c`) + relink** path works on-device (B11-3d had only done the one-shot compile+link). The `.frs` is registered in `DIAGRAMS` (committed `builddriver.svg`, `check-diagrams` clean) with a `docs/systems/builddriver.md` doc, matching every other Frame system. Validated by the `console-test` `/bin/buildc` step. This closes the B11-3 track: **the OS compiles + links + runs C with its own toolchain, orchestrated by a Frame state machine** — the V1.0 C-toolchain north star, with Frame holding the lifecycle.
- **2026-05-25** — **S5/S6 (shell I/O: redirection + pipes) — the I/O-sequencing saga, and a *supervisor* FSM (`IoScheduler`): the first time a Frame FSM was reached for as a *fix*, not a feature.** The shell-command track added `>`/`>>`/`<` redirection (S5, a per-process fd table + `dup2`) and `|` pipes (S6, an anonymous-pipe buffer + a forked writer/reader pair). Functionally small; what makes them worth a long entry is that they were the **first features to force genuinely concurrent + blocking I/O to compose** — a forked child creating a file then exec'ing, two pipeline children exec'ing *simultaneously*, a reader blocking until a writer produces — and that flushed a **cascade of stateful-sequencing bugs**, every one of which I first tried to fix with *scattered native coordination* and got wrong:
  - **S5 redirect was intermittently silent** — `echo foo > /r.txt` sometimes left `/r.txt` uncreated (the next `>>` then made it fresh, so it looked like truncation). 
  - **S6 pipes failed deterministically** — `echo … | wc` reported the writer as "command not found": two children exec'ing at once **clobbered the single-flight virtio scratch buffer**, so one child's ELF read returned corrupt bytes (this also retro-diagnosed the long-standing "concurrent_exec_buffers ~25% flake").
  My first patches were exactly the anti-pattern the project keeps warning about: a `DISK_BUSY` bool + a hand-rolled waiter array + per-syscall `PENDING_*` statics + a lost-wakeup-prone `block_current`. **The maintainer's intervention is the finding: "you have stateful things that aren't sequenced right — you need a supervisor FSM to coordinate."** That reframed the disk engine's *access* as a lifecycle and produced `frame/io_scheduler.frs` — a supervisor with `$Idle`/`$Busy` states + a waiter queue in its domain that owns "who holds the single-flight engine, who's queued, who's next" (`acquire`→grant-or-enqueue, `release`→hand-off-or-idle). `sched.rs` drives it; native only does DMA + "block until I'm the owner" + "wake the next owner." A second small FSM, `frame/pipe.frs`, models the pipe end-lifecycle (`$Writable`→`$Drained`, writer-presence as the state that decides read-blocks-vs-EOF — the `OpenFile` shape). With the supervisor serializing transactions, **S5 and S6 both became reliable.**
  - **A new Frame shape, and a genuinely positive one.** Every prior Frame win in this codebase was a *per-instance lifecycle* (TcpConnection, UsbEnumeration, OpenFile). `IoScheduler` is the first **coordinator/supervisor**: one shared instance arbitrating a contended resource across many processes — the kernel's analog of an actor that owns a mutex+queue. It's a clean fit (the access protocol genuinely *is* state-shaped: idle vs busy dispatch `acquire` differently), and modeling it as states+queue made the serialization + fair hand-off correct-by-construction, where three native attempts had subtle lost-wakeup / clobber bugs. This is the strongest case so far that Frame helps not just with *protocols* but with *coordination logic* — the bug class that scattered atomics are notoriously bad at.
  - **But the honest headline reinforces finding #2, hard: the genuinely difficult bug was native, and the FSM did not prevent it.** While bisecting a regression the I/O work introduced (on-device **tcc** stopped producing `/out.elf`), the root cause turned out to be *my* native completion-detection change, nothing to do with Frame: I had `wait_and_drain` poll the device-written **status byte** as "done," but virtio only guarantees all of a request's buffers (data *and* status) are written once it advances **`used.idx`** — the per-buffer write order is unspecified. So a poll could see status before the data DMA landed. Invisible for a 1-sector S5 read; tcc's heavy multi-sector reads got occasional stale bytes → corrupt output. The fix is pure native (poll `used.idx`, then read status). So the supervisor FSM cleanly owned the part it was given (access sequencing) — but the *hard* bug lived one layer down in the driver's understanding of the hardware contract, exactly where this assessment has said all the difficult bugs live (paging, DMA, the Frame↔native boundary), never in the state logic.
  - **The ISR constraint bit again, and bounded the design.** The supervisor can coordinate only **syscall-context** events (disk release/hand-off, pipe write/close, child-exit); the **IRQ-context** wakes (disk completion, console RX) *cannot* touch Frame — the standing "ISR never dispatches a Frame system" rule (non-reentrant, allocating). So those wakes stay native `wake_pid` shims and the woken process re-checks the FSM/condition. A fully-unified "every blocking wake routes through one FSM" is therefore physically impossible here; the achievable unification is "the FSM owns the *logic* (who waits, who's next), native owns the *mechanism* (mark Blocked, flip Runnable, DMA)" — the same split as B7's cross-core post/drain, now for in-kernel I/O arbitration.
  - **Net.** S5/S6 are a *positive* for Frame on a new axis (supervisor/coordination, not just lifecycle) — and simultaneously the cleanest illustration yet of the project's central division: reaching for the FSM fixed the sequencing that native flags kept getting wrong, while the one truly subtle bug was a native hardware-contract error the FSM had no bearing on. (Validation status, honest: S5 + S6-pipe confirmed green with the supervisor; the `used.idx` tcc fix builds clean and is spec-correct but its end-to-end pass is pending a clean run — the arm64→x86 TCG host was too throttled by repeated runs to complete one when this was written.)
- **2026-05-25 (follow-up) — the "used-ring" fix, and a clean statement of *why it was not Frame's to solve* (the sharpest native/Frame boundary case yet).** The subtle bug above was finally root-caused and it turned out to explain *both* symptoms (tcc's corrupt output **and** S5's intermittent lost-create) with one cause — worth recording precisely because the fix is emphatically *native*, and articulating why is the most useful Frame-boundary lesson of the episode.
  - **The bug.** virtio-blk completion was being detected by polling the request's **status byte**. The virtio spec only guarantees the device has written *all* of a request's buffers (data **and** status) once it advances the used-ring index **`used.idx`**; the order it writes the individual buffers is unspecified. So a poll on the status byte could observe "done" before the data DMA landed → a stale read. Invisible for a one-sector read (S5's tiny files mostly), lethal for tcc's hundreds of multi-sector reads (corrupt ELF). The *same* stale-read race also intermittently fed `fs::create` a stale `root.size`/dir-block, misplacing a directory entry — which is why "create looked perfect (immediate re-lookup found the file) yet a later `cat` couldn't" (diagnostics showed `relookup=57` at create, failure only afterward). **Fix:** wait on `used.idx` advancing (the spec's "everything is written" signal), then read data + status. ~3 lines. 14 consecutive end-to-end passes followed.
  - **Why this is NOT a Frame problem — three converging reasons, each already a standing finding here.** (1) *The lifecycle is already Frame.* `BlockRequest` (`$Queued→$InFlight→$Complete/$Error`) models the request; the used-ring poll is the native *mechanism* that observes the hardware and fires `complete()`. Frame owns the lifecycle; native owns the eyes. (2) *The bug lives below Frame's abstraction.* "`used.idx`, not the status byte, is the completion signal, and it's written last" is a **hardware-contract fact** about virtio. No state machine can *express* it; whatever fires the `complete()` transition still has to read the correct register in native code. This is finding #2 in its purest form — the difficult bug was at the Frame↔native boundary (the native code that feeds the FSM), never in the state logic. (3) *The used ring is a hardware DMA ring, not a software queue.* The **R7c** finding already drew this line: hardware→software transports (the virtio used-ring, the xHCI event ring) are device-defined and owned by native code; only software→software deferred dispatch is the Frame-mailbox case. Modeling the used ring in Frame would duplicate the hardware.
  - **Its "state" is a cursor, not a machine.** The only state the fix carries is `d.last_used` — a monotonic index into the used ring, compared against the device's `used.idx`. That's a counter (like a heap break or a ring head/tail), not a lifecycle with mode-dependent dispatch, so it fails the project's bar for an FSM (no event means different things in different states).
  - **The contrast is the lesson.** This episode had *two* stateful problems one layer apart, and they wanted *opposite* tools. The **access coordination** (who holds the single-flight engine, who's queued, who's next) is genuinely mode-shaped — idle vs busy dispatch `acquire` differently — and belonged in Frame (`IoScheduler`); doing it with native flags was the mistake that produced the lost-wakeup/clobber bugs. The **completion detection** (which hardware word means "done") is a hardware contract with no modes, and belonged in native; trying to push it into Frame would have been ceremony that still couldn't have prevented the bug. So the same episode is simultaneously a Frame *win* (the supervisor) and a clean example of Frame *correctly staying out* (the used-ring poll) — and the discriminator is exactly the assessment's running thesis: **Frame for lifecycle/coordination state where events mean different things per mode; native for mechanism and for every fact about the hardware itself.**
- **2026-05-25 — tooling: the diagram-drift gate now gates the DOT, not the rendered SVG (a Frame-*tooling* finding).** Adding the two new systems above surfaced a fragility in `check-diagrams`: it byte-compared the **rendered SVG** (`framec -l graphviz` → `dot -Tsvg`) against the committed one. But an SVG's bytes depend on the **GraphViz version** (coordinates, the `Generated by graphviz X` comment), not just the FSM — so the committed SVGs (GraphViz 2.43.0, matching CI's apt) couldn't be regenerated on the dev container (GraphViz 14.1.5) without churning all 28 with version-only diffs, and the gate effectively **pinned the whole repo to one GraphViz**. That's a gate failing on a *toolchain* difference, not a correctness one — it blocked committing the new diagrams. **Fix:** gate on the **DOT** (`framec -l graphviz`), which is the FSM's structure and is *version-independent* — verified byte-identical across framec 4.2.1↔4.2.3 and across host/container. `check-diagrams` now compares a committed `<system>.dot` (the source of truth); the `.svg` is a *render* committed only for GitHub display, no longer drift-gated. Bonus: the check needs **only framec, not GraphViz at all**, so CI's diagram job no longer depends on a GraphViz version. The general lesson (beyond this repo): when you gate generated artifacts, **gate the deterministic stage, not the rendered one** — the FSM→DOT step is a pure function of the source; the DOT→pixels step is a tool's layout choice. The Frame-specific upshot is that framec's graphviz output is a stable, gateable description of a system's graph, which is the right drift anchor for "the committed diagram still matches the FSM."
- **2026-05-25 — S8 (`mv`/rename): a kernel-stack overflow that was 100% native codegen, with nothing for Frame to own — and a debugging note on reading a fault that lands *in data*.** Adding the rename syscall (a `fs::rename` re-pointing the inode, plus syscall #26) made the kernel start faulting **deterministically** right after `ls /usr` — i.e. on a code path the new syscall never touches. The fault was a `#PF` at a *fixed* address, `0xffffffff80133fc8`, which `nm` placed **inside `.bss`** (just past `allocator::HEAP`): RIP had jumped into non-executable kernel data, the signature of a clobbered return address / corrupted control flow, not a logic error.
  - **The cause was pure Rust/LLVM stack codegen.** `perform_syscall` is one big `match`; seven-plus arms each declare a `let canon = [0u8; 512]` path buffer, and the #26 arm declared *two* (src + dst). In a **debug build (opt-level 0) LLVM performs no stack-slot coloring**, so every arm's buffer gets its *own* slot in the single `perform_syscall` frame — they all coexist on the stack regardless of which arm actually runs. The two extra 512-byte buffers pushed the cumulative frame past what the **16 KiB kernel stack** (`gdt::KERNEL_STACK`) could hold on a deep path (`readdir` → `fs` → block layer), so a spill/return overwrote a neighbour and RIP went into BSS. That's why a syscall the path never calls could crash an unrelated path: the frame size is a *static* property of the function, paid by every arm.
  - **Bisection did the work; the FSM had no part in it.** Clean S7 HEAD passed fully; reverting *only* the #26 arm made the crash vanish; toggling `INODE_BLOCKS` (the first, wrong guess — inode exhaustion) and the on-disk `/bin/mv` file changed nothing. The discriminator was the *fixed* fault address: a wild/heap-corruption bug wanders, a blown return slot repeats, and `nm` turning that address into "inside `.bss`, just past `HEAP`" is what named it a stack overflow rather than a logic fault.
  - **The fix (maintainer's call: do it for the whole class, not just #26).** Each path-resolving arm (`open`/`stat`/`unlink`/`chdir`/`getcwd`/`readdir`/`mkdir`/`rmdir`/`rename`) moved into its own `#[inline(never)]` helper, so a 512-byte buffer only occupies the stack while *that* syscall runs and `perform_syscall`'s frame shrinks to pure dispatch. This restores the proven-good footprint and future-proofs S9/S10 (more arms) against re-tripping the same ceiling — the right fix for "big per-arm buffers in one `match`," versus the band-aid of just enlarging the stack.
  - **Why this has nothing to do with Frame — and that's the point.** There is no lifecycle, no coordination, no event-means-different-things-per-mode here; it's a fact about how `rustc -O0` lays out a stack frame against a fixed kernel stack size. No state machine could express it or prevent it. It sits exactly where this assessment keeps finding the genuinely hard bugs: in native code and the machine's own constraints (paging, DMA, stack sizing, codegen), never in the state logic. S8's *feature* logic (`fs::rename` re-pointing an inode: link-new-name then clear-old, file-overwrite ok, dir-clobber refused) was correct on the first try and needed no debugging at all — the entire episode was a native systems-programming bug wearing a Frame-shaped task as a costume.
  - **Validation, and an honest note on the harness, not the kernel.** Confirmed by the interactive `console-test`: `echo movesrc > /msrc; mv /msrc /mdst; wc /mdst` → `1 1 8 /mdst` (content now at the destination) and `cat /msrc` → `cannot open /msrc` (source name gone) — a full green run end-to-end (`#PF` gone across 6+ runs). Getting that green run, though, cost several false failures that were **purely the test environment**: the arm64→x86_64 **TCG** emulator (no hardware virt for an x86 guest on Apple Silicon) runs the guest on one CPU-bound host thread, so when a couple of greedy desktop processes crowded it the *emulated* clock fell behind the harness's wall-clock needle timeouts → random early-step timeouts; and a **stray container from a bash-timeout-killed run** left a QEMU holding the console socket, so the next run booted to an *empty* capture. Both are infrastructure noise, not kernel bugs — but they're a standing reminder that on this host "a needle timed out" is ambiguous until you separate throttle/leftover-process from a real hang (here, distinguished by: a clean run sails through, and the disk acquire/hand-off paths re-checked as lost-wakeup-proof).
- **2026-05-25 — S9 (`ps`) + the shell-serialization saga: a "disk race" that was really a `wait()` bug, then a lost wakeup, then a diagnosis wall — and zero of it was Frame's to own.** S9 (`ps`: a `sched::live_procs` snapshot syscall + `/bin/ps`) was trivial. The episode worth recording is the intermittent failure its console-test run surfaced, which had been mis-attributed to "the disk" for two prior journal entries.
  - **The mis-diagnosis.** `echo foo > /r.txt; cat /r.txt` intermittently showed `cat: cannot open /r.txt`, and `mv` intermittently left the destination missing — looking exactly like the old S5 "intermittent disk-create loss." A create-time probe (compare the root dir's `size` read cached-vs-uncached-off-disk) killed that theory cold: **the disk was always consistent** (cached == on-disk every time). The real signal was the fork/exit/reap ordering: a command ran *before* its predecessor's effect existed.
  - **Bug #1 (the dominant ~50%): the shell ran ahead of its own children.** `ish`'s `run_external` called bare `wait()` (reap *any* child), not `waitpid(the child it just forked)`. Once two children briefly overlapped, `wait()` reaped an *older* one and returned while the just-launched command was still running — so the next command (often an in-process builtin like `cat`, which can't be re-ordered by the scheduler) raced ahead and read a file that hadn't been written yet. Fix: a `waitpid(pid)` form of the wait syscall (`0` = POSIX wait-any, preserved for the other test programs) and `ish` waits for the *specific* child. Structurally ≤1 outstanding child ⇒ no run-ahead. **~50% → ~90% green, zero ordering-misses across 90+ runs.**
  - **Bug #2 (exposed by the fix): a lost wakeup in `wait`.** `do_wait_loop` used the old `block_current` — a *check-then-block with a gap*, so a child exiting in that gap had its wake lost. Bare `wait()` had hidden it (it usually found an already-dead child and never blocked); `waitpid` made the shell actually block for a *specific* child, surfacing it as an occasional permanent shell hang. Fix: rewire `wait` to `block_current_until` (the atomic check-and-block the disk path already used) and **delete `block_current` outright** so nobody can reach for the racy primitive again. The lesson is primitive hygiene: there must be exactly *one* correct blocking primitive, used everywhere.
  - **Is concurrency a job for a supervisor FSM? Here, no — and the reason is the running thesis.** The maintainer asked (fairly, given the IoScheduler precedent) whether wait/process coordination wanted a supervisor FSM. It doesn't: a supervisor fits a *contended shared resource* with mode-dependent dispatch and a waiter queue (the single-flight disk — `IoScheduler` ✓). `wait` has none of that shape — each parent waits for *its own* children independently; there's no resource to arbitrate. And both bugs were squarely native mechanism: a ring-3 *policy* choice (`wait` vs `waitpid`) and an interrupts-off *atomicity* fact (`block_current_until`) that Frame literally cannot express and whose wake fires from contexts forbidden to dispatch a Frame system. Supervisor FSM for contended-resource coordination; native atomic primitive for a per-process blocking condition.
  - **The residual (~6–10%), and an honest wall.** A rare intermittent *wrong-result* in the `mv`/redirect path remains: the op completes but does the wrong thing, the test's needle never prints, and the shell idles. It is **not** a hang (watchdogs on every kernel block-point — `read_line`, `wait`, disk-acquire, disk-completion — never fired) and **not** throttle (it fails even at 300 s needle timeouts). The blocker is the **diagnostic channel itself**: the serial console drops/truncates output under exactly the load that triggers the bug, and an in-memory event tracer didn't help because *its dump also goes over serial*. Worse, the watchdogs' own dumps perturbed timing enough to inflate the failure rate (67% with them, ~90% without). The genuine next step is **QEMU-monitor memory inspection** (dump the trace ring straight from guest RAM, bypassing serial) — scoped as a follow-up. Recorded plainly because it's the project's recurring shape in its sharpest form: the hard, still-open bug lives in native mechanism below Frame's abstraction, and even *observing* it is a native/tooling problem, not a state-logic one.
- **2026-05-26 — #110 follow-up: an out-of-band diagnostic finally *saw* the residual, and it's a virtio-blk completion bug (not shell logic) — plus a failed-fix lesson.** The S9 entry left the residual ~6–10% flake "needs QEMU-memory inspection." This is that inspection and its outcome.
  - **The diagnostic (the reusable win).** Serial is the only guest→host channel and it drops/truncates output under exactly the load that triggers the bug — so live prints, the tail, *and* an in-RAM ring dumped over serial were all useless. The fix: a kernel event ring written to a **fixed guest-physical frame** (announced once at boot, when serial is reliable), read straight out of guest RAM by the harness via the **QEMU monitor's `pmemsave`** (QMP, a separate channel that can't be lost). With FORK/WAITSYS/WL_ENTER/REAP/EXIT/CREATE/RENAME records (+ a shell-issued `MARK` syscall so the *shell's own* control flow is in the trace), the residual became observable for the first time. General lesson: **when the bug corrupts your only telemetry channel, get telemetry out-of-band** — here, host-side memory inspection of a fixed guest address.
  - **The verdict: it's the disk, not the shell.** The clean traces are unambiguous — every command's `FORK → WAITSYS → WL_ENTER → REAP` pairs correctly (the waitpid serialization fix holds), yet a command still produces the wrong result or hangs. Two faces of one cause in the virtio-blk **completion** path: (a) *stale read* — `read_sector` returns pre-write bytes, so `wc < /r.txt` counts wrong; (b) *lost completion* — `used.idx` never advances for a request (e.g. `touch` hung inside `exec`'s disk read), hanging the process. Same `used.idx` hardware-contract class an earlier journal entry "fixed" — reduced, not eliminated, on this **arm64 host emulating x86 via TCG**, where DMA-write-vs-`used.idx`-update ordering isn't the strong TSO real x86 gives.
  - **The failed fix, recorded so it isn't retried.** Two changes, both reverted: (1) **re-notifying** the queue on a stalled wait *regressed* the pass rate to ~12% — re-kicking a *single-flight legacy* virtqueue makes QEMU re-process the same `avail` entry → double-completion → `used.idx` desync → broad stale reads. **Do not re-kick a single-flight queue.** (2) A `SeqCst` read-fence before consuming the buffer was neutral. The takeaway is a discipline one: a virtio completion bug entangled with the host's emulated memory model is not fixable by guessing at fences/kicks — it needs the used-ring *element* (id/len) verified per the spec, validated over many runs on an unloaded host. Left as tracked task #110 with the diagnostic reconstructable from history.
  - **Where it sits in the thesis.** This is the project's running shape in its sharpest form yet: the dominant, frequent bug was *shell/process-lifecycle* logic and got fixed cleanly (waitpid + lost-wakeup-proof block); the stubborn residual is a *native hardware-contract* fact (virtio completion ordering) below any abstraction Frame could express — and even *observing* it was a native/tooling problem, not a state-logic one. Frame for lifecycle/coordination; native for the mechanism and every fact about the (here, emulated) hardware itself.
- **2026-05-26 — S10 (job control + full POSIX signals): the cleanest Frame/native split the shell track has produced, and a precise rule for *which* FSM owns *which* state.** S10 is the largest shell step — `&`/`jobs`/`fg`/`bg`/`kill`, plus the signal subsystem under them (`kill`/`sigaction`/`sigreturn`/`sigprocmask`/`set_foreground` syscalls #29–#33, default actions, user handler trampolines, job-control stop/continue, and Ctrl-Z/Ctrl-C terminal signals). It was built and validated in four increments (`&`+jobs+fg; kill+terminate; handlers+sigreturn; stop/continue+Ctrl-Z/bg/fg-resume), each green in the dev container before the next.
  - **The job-control lifecycle is a Frame FSM — `IshJobs`, in ring 3.** The genuinely stateful, behavior-gating part of job control is the shell's session model: a job is foreground / background / stopped, and the shell itself is at-prompt vs. blocked-on-a-foreground-job — and those modes change what `fg`/`bg`/`jobs`/the prompt-time harvest *do*. That earned a `.frs` (`frame/ish_jobs.frs`: `$Idle`/`$Foreground` + a `Vec<JobEntry>` table), notable as the **first Frame system written for a ring-3 program** (it compiles for `x86_64-unknown-none` and is `include!`d by ish exactly as the kernel includes its systems). It's the ish-resident adaptation of the hosted-shell `job_control.frs` — but it does *not* spawn jobs (ish forks/execs/waitpids through the kernel); the FSM is a pure coordinator that tracks pids the shell created.
  - **The rule the kernel side forced into focus: transient run-state is NOT an FSM state.** The natural temptation was to add `$Stopped` to the kernel's `Process` lifecycle FSM. Reading `process.frs` stopped that: it *deliberately* omits `$Running` and doesn't even track `$Blocked`, with the header's reason — "Currently on the CPU flips on every timer tick and the ISR cannot fire Frame events." Three forces make this correct, not a shortcut: (1) those transitions fire from the timer ISR / device IRQs, and Frame dispatch is non-reentrant, so you *can't* drive them from an FSM; (2) the scheduler must own runnability — it reads it on the hottest path with interrupts off — so duplicating it into the FSM means lockstep overhead or drift, i.e. two sources of truth for one fact; (3) runnability *gates no event handling* (unlike "reap only a zombie" or "kill from any live state"), so an FSM state for it would be ceremony with no invariant to enforce. So `Stopped` went in as a native `RunState` (the exact mirror of `Blocked`), and the *lifecycle* FSM stayed the shell-side `IshJobs`. This is the inverse of the `feedback_block_transform` lesson: just as you don't fake a state machine with ad-hoc string matching, you don't force a non-state-machine fact (which thread is on-CPU) *into* one. The FSM owns "what is this job's role in the session"; the scheduler owns "can this thread run right now."
  - **Everything else was mechanism, and stayed native — correctly.** Pending/blocked **signal bitmasks** + the **handler table** are TCB fields (bit ops, not a graph). The **handler trampoline** — push the interrupted `TrapFrame` on the user stack below the red zone, redirect RIP to the handler with `rdi`=signo, push the restorer as the return address, and `sigreturn` restoring the frame out-of-band ahead of the dispatcher — is pure register/stack choreography. **Delivery** rides the existing syscall-return boundary (a blocked target is flipped Runnable by `send_signal` so it returns to take it); the **WUNTRACED** wait-return that lets the foreground shell regain its prompt on Ctrl-Z is one added predicate (`child_stopped`) on the same lost-wakeup-proof `block_current_until`. None of it is a state machine; all of it is the "~70% of a kernel Frame doesn't touch" the assessment has named since R5. The division of labor held across the biggest shell feature yet without strain — which is the positive result worth recording.
  - **Validation + the standing caveat.** Each increment is a console-test section driven over serial: `kill %2` → `exited with code 143`; `sigtest` handler → `handler: caught` → `sigreturn` → `resumed`; `kill -STOP/-CONT` → `stopped (sig 19)`/`continued`; `spin fg2c` → Ctrl-Z → `stopped (sig 20)` + shell `[5]+ Stopped` (WUNTRACED) → `bg %5` → `jobs` Running. Kernel-log lines are used as oracles only where program output can't observe the effect (a suspend has no stdout), with signal numbers chosen to keep each needle's first occurrence unique. One run hit the tracked #110 virtio flake at the unrelated `mv` step and passed clean on re-run — the residual remains a native disk-completion fact, untouched by and unrelated to S10.
- **2026-05-26 — #110 revisited under S10: separating *self-inflicted test fragility* from the *irreducible host artifact*, and a discipline win (revert the neutral guesses).** A clean re-measure on an unloaded host (load ~3) put the console-test flake higher than the old "6–10%" estimate — ~3/8 first pass — so I dug in run by run. The failures split into two unrelated causes, and telling them apart was the whole exercise.
  - **Two self-inflicted flakes, both real bugs, both fixed.** (1) The S10 job-control test sections leave *background processes* alive; killing one with `kill %N` doesn't reap it, so a leftover `spin` died during a *later* section (cmain) and its kernel exit log interleaved into cmain's stdout, splitting a needle. Fixed by terminating each test job with a *unique* signal (`-INT`→130, `-KILL`→137) so the harness can wait on its distinct exit code — which keeps the shell idle long enough for the job to die — then a `pwd` harvests it before moving on. (2) Worse and more general: ish's `print` writes **one byte per `write_char` syscall**, so an async kernel log (a signal-delivery trace firing *because* of job control) could interleave *between* bytes and split a line — `] Running` became `] R`+`unning`. Fixed by building each job-control report line in a buffer and emitting it in **one `write` syscall** (the console write is atomic — a process's line is never split mid-way, per the S5 used-ring journal). Both are genuine robustness fixes the job-control tests *exposed* (the async-log-vs-byte-at-a-time-output race pre-existed; nothing before generated logs mid-print).
  - **The irreducible core: a virtio-blk *no-completion*, confirmed and left alone.** With the test flakes gone, every remaining failure is the same one: the `mv`→`wc /mdst` step *hangs* (the shell forks `mv`, then no further output) — `used.idx` **never advances**. This is the lost-completion face from the prior #110 entry: QEMU's device thread, on this arm64-MTTCG-emulating-x86 host, simply never delivers the completion for a request. Rate measured at ~2–5/16 across batches with no trend — it's a host fact, not a guest-logic one.
  - **The discipline win: I tried three guest-side fixes, measured them neutral over ~40 runs, and reverted all three.** (a) A `fence(SeqCst)` *inside* the completion poll (not just after the wait, as the prior failed attempt did) — the hypothesis being a stale `used.idx` re-read on wake; (b) reading the used-ring *element* (id/len) per spec; (c) making `block_current_until` re-poll `ready()` on *every* interrupt (timer included), so a *dropped completion IRQ* couldn't hang a wait whose completion had in fact landed. Each is individually defensible, (c) especially (it's lost-waker-proofing a load-bearing primitive). But the measured rate didn't move — because the bug is a completion that *never happens*, so there's nothing for a barrier to make visible or a re-poll to observe. Keeping changes that don't fix the thing they target, on a critical primitive, is exactly what the prior entry warned against ("not fixable by guessing at fences/kicks") — so they were reverted. The honest state: #110's core is a QEMU/TCG virtio-completion limitation **below any abstraction the guest can express**, the same conclusion the out-of-band-diagnostic entry reached, now reconfirmed with clean numbers and with the *surrounding* (guest-side, genuinely-fixable) flakes peeled away. The lesson worth keeping: when a flaky test has a hardware-artifact core, the high-value work is isolating and fixing the *self-inflicted* noise around it so the artifact's true rate is visible — not piling guest-side guesses onto the artifact itself.
- **2026-05-26 (correction) — the residual is NOT a "no-completion hang"; it's a *completed-but-incoherent* disk op, and I mis-stated it above.** A deeper instrumented pass corrected the previous bullet's framing, which I'd asserted too confidently. Two findings forced the correction:
  - **There are two faces, and the dominant one is a *wrong result*, not a hang.** Watchdog instrumentation on the disk wait *and* on `block_current_until` (the primitive backing both the shell's `waitpid` and the disk wait) **never fired** across many reproductions — so the failing `mv` is not stalled in any wait, which means the disk request *does* complete (`used.idx` *does* advance — the earlier "never advances" was wrong). The frequent face is: `mv` exits **0** (so `fs::rename` ran and returned `true`), yet `/mdst` doesn't exist and `/msrc` still holds its content. So `rename`'s block writes (`dir_link` + `clear_dirent`) reported success but **didn't persist** (or a later read of those blocks returns stale bytes). The write-through buffer cache masks it until a sibling process (`wc`'s `exec` reading `/bin/wc`) evicts the `/`-dir cache slot; the re-read from disk then returns the pre-rename block → `wc: cannot open '/mdst'`. That is the **stale-read / lost-write** class the *original* #110 entry named — so this is the same artifact, just observed at the fs layer, and my "no-completion / hang" framing two bullets up was a wrong turn.
  - **Serial instrumentation is unreliable for exactly this bug — as the out-of-band-diagnostic entry already warned.** Breadcrumbs added to `sys_rename` and `fs::rename` printed **zero** lines on a failing run where the kernel had demonstrably recompiled with them and `mv` had demonstrably run — because the serial channel drops output under the trigger load. So every "trace says X didn't run" inference from serial is untrustworthy here; pinning the exact failing block write requires the in-RAM-ring + QMP-`pmemsave` tracer the prior entry built, not `serial::writeln`. All diagnostic instrumentation from this pass was reverted (it relied on serial and/or the BSP tick counter, both of which the failure mode defeats).
  - **Net:** #110 stands where the original out-of-band entry left it — a QEMU/TCG-on-arm64 virtio-blk data-coherency artifact (a write that doesn't durably land, or a read that returns stale bytes, on a request that *does* complete), below any guest abstraction — not the "completion never arrives" hang I briefly (mis)characterized. The self-inflicted test flakes are genuinely fixed; the disk artifact is not, and isn't guest-fixable by barriers/re-polls. Proper next step if revisited: the out-of-band tracer to capture the exact block + its on-disk bytes across the write→evict→reread window, and/or move off the legacy single-flight virtqueue.
- **2026-05-26 — S10 shortcut audit ("are we excellent within scope?"): two real correctness bugs found + fixed, and the no-Frame-in-ISR invariant *shaping* a fix rather than being hacked around.** Prompted by a "why legacy / have we shortcutted anything" review, I audited the S10 work (and the disk stack) for hidden hacks. The honest split: a few deliberate *scope simplifications* (legacy virtio, single-flight disk, BSP-only ring-3 scheduling, the fs buffer cache being unlocked — safe only *because* fs is BSP-only, now commented), and two genuine bugs I'd left in S10, both fixed:
  - **Bug 1 — a foreground `waitpid` silently reaped finished background jobs.** `do_wait_loop`, on a specific-pid wait, drained *all* dead children (to avoid zombie pileup) and returned only the target's status — so a finished `&` job got reaped out from under the `IshJobs` FSM, its `[id]+ Done` report lost and its entry stuck at "Running". Fix: `waitpid(target)` reaps *exactly* its target (`sched::reap_dead_pid`); other dead children stay zombies for the shell's prompt-time harvest (`reap_nohang`), the path that *does* notify the FSM. Also more correct POSIX. The first console-test job-control case now triggers its harvest with a *foreground external* command (which forks + is waited on) instead of a builtin, so it guards this directly.
  - **Bug 2 — signals delivered only at the syscall-return boundary**, so a CPU-bound process with no syscalls (and no faults) was unkillable. The clean fix collided with a *deliberate, load-bearing invariant*: the timer ISR must never dispatch a Frame system (Frame dispatch is non-reentrant; the timer can preempt a syscall mid-dispatch). A terminate's natural path — `proc_table().exit_pid` — *is* a Frame dispatch, so it can't run from the timer. **Rather than weaken the invariant, the invariant shaped the design:** a vDSO-style `sigtramp` page (one RX-user page, `mov eax,1; syscall`, mapped into every address space by the ELF loader, copied by fork, freed per-AS). On a timer preempt of a ring-3 process with a pending signal, `schedule()` calls a *native-only* deliverer that, for a terminate, just rewrites the process's RIP to the trampoline (rdi = exit status) — so the process calls `exit()` **itself** at its very next instruction, and `do_exit` (the Frame dispatch) happens at the syscall boundary where it's legal. Stop marks it `Stopped` (no park) + SIGCHLD; handler does the usual frame rewrite. This is the more general statement of the kernel's whole Frame/native line: *the ISR is a place where the FSM machinery cannot run, so work that needs it is bounced to a context where it can* — here by making the target trap itself into the kernel. Validated by `/bin/busyloop` (zero syscalls) being killed via `kill -6` (exit 134) — only the timer path can reach it. (Bonus: the "continued" log unified into `send_signal`'s SIGCONT-resume, so the syscall-park and timer-mark stop paths report identically.)
  - **The audit's positive result:** no hidden hacks — the simplifications are honest scope boundaries (documented), and the two real bugs are fixed with mechanisms that *fit* the architecture (reap-only-target; bounce-terminate-to-the-syscall-boundary) rather than fighting it. The legacy-virtio / single-flight ceilings remain open, deliberate scope items — not shortcuts masquerading as done.
- **2026-05-26 — #110 RESOLVED (its *nature*, not the host bug): a write-verify probe proves writes are never lost — it's a stale READ — so modern virtio would NOT fix it.** Before committing to the "modern virtio-blk" rewrite, I built the out-of-band probe the prior entries kept deferring — but a simpler form than full QMP `pmemsave`: the trace lives in RAM and is dumped over serial *after* the storm (when serial is calm), and the sharpest probe is a **write-then-read-back verify** inside `write_sector` — after each write completes, read the sector back *below the fs buffer cache* (reusing the held single-flight engine) and compare its on-disk checksum to what was written. Counts + failing sectors are dumped via a `dbgdump` syscall the console-test invokes right after the `mv` step.
  - **The result is unambiguous.** Across every failing `mv` run that reached the dump: **`writes=39 lost=0`** — *zero* lost writes, ever, over hundreds of writes. Yet on those same runs `mv` produced the wrong result (`wc: cannot open '/mdst'`, `cat /msrc` → `movesrc`). So the rename's *writes landed and were verified correct on disk*; the failure is that a *later read* of a written sector returns **stale, pre-write bytes**. (A separate, rarer face is the hard hang, where the dump never prints — a read feeding the fs into a wedged state, same root.) This is a stale-**read** artifact, not a lost write.
  - **Why this is decisive for the roadmap:** a stale read from the emulated device on the arm64-MTTCG host is *below any guest abstraction*, and — critically — **modern virtio-blk (1.0) would not fix it**: it uses the same used-ring/AIO read path in the same QEMU device thread. So the evidence *prevents* a multi-day driver rewrite that would not have moved the needle. The honest fixes remain environmental: real x86 hardware (where cache-coherent DMA + PCI ordering make this impossible), a different QEMU accel, or a QEMU/MTTCG fix. The guest is doing everything correctly — it writes, it can even verify the write landed, and a subsequent read of that same sector still comes back stale.
  - **Discipline note:** the write-verify is a *diagnostic* (it doubles disk ops), so it was reverted after extracting the finding — the technique is reconstructable from this entry. The value delivered is the *evidence*: #110 is conclusively a host-emulation stale-read, writes are sound, and the modern-virtio rewrite is the wrong fix. "Build the probe before the rewrite" earned its keep.
- **2026-05-26 — #110 "can it be fixed?": three guest-side hypotheses generated, tested, and FALSIFIED — strengthening (not overturning) the host-artifact verdict.** A fresh, skeptical pass on the *hang* face (the `wc /mdst` child blocks forever, no output) rather than the stale-read face. Each hypothesis had a clean theoretical story and a decisive experiment:
  1. **Missing `virtio_rmb` (read barrier on the completion path).** The driver polls `used.idx` and reads status/data with no acquire fence — a real virtio-spec gap that is a no-op on x86/TSO but bites on weak-memory arm64. Added the fence both before the `used.idx` read and before the data read. **Result: still hung.** Not the (sole) cause.
  2. **MTTCG inter-vCPU ordering.** Ran the identical kernel under `-accel tcg,thread=single` (added an opt-in `FRAMEOS_QEMU_ACCEL` hook in xtask). **Result: hang persists single-threaded** (and once at a *different* command — the S5 redirect — proving it's generic disk completion, not rename-specific). So it is **not** an inter-vCPU MTTCG race.
  3. **Lost completion IRQ.** A blocked I/O waiter is revived *only* by `wake_pid` from `on_irq`; one dropped edge-triggered 8259 interrupt would park it forever. Added a timer-ISR **completion-poll fallback** (`poll_completion_wake`) that re-checks `used.idx` every tick and wakes the waiter even if the IRQ was lost. **Result: still hung** — and this is the decisive one: if the I/O had completed (`used.idx` advanced) and only the IRQ were lost, the 100 Hz poll would recover it in ≤10 ms. It didn't → **`used.idx` never advances → the request itself isn't completing**, not its notification.
  - **The meta-finding that reframes everything: the failure rate is non-stationary and tracks HOST LOAD.** Across this session's ~25+ back-to-back QEMU/TCG runs the rate climbed monotonically (0% fresh → ~80% saturated) with guest code held constant for long stretches. That (a) confounds every sequential A/B test — no guest fix can be validated by counting pass/fail on a drifting baseline — and (b) is itself strong evidence the dominant trigger is the **saturated software-emulation host**: QEMU's block-AIO completion path starving under load (request submitted, `used.idx` never bumped within the 90 s window). Consistent with, and a sharper substantiation of, the earlier stale-read conclusion: both faces (stale read; never-completes hang) live in QEMU's emulated-device read/completion path, below the guest.
  - **Verdict on fixability:** the cheap guest-side fixes the symptom *suggested* (barriers, lost-IRQ recovery) were each built and **empirically falsified** — important negative results that rule them out. The `virtio_rmb` barriers are still correct spec-hygiene worth adding on their own merits (just not a #110 fix). The honest answer stands: **not cheaply guest-fixable; the trigger is host/QEMU emulation under load.** Real fixes remain environmental (x86 hardware, KVM, a lighter host, or a QEMU/MTTCG fix). All three experiments were reverted (unvalidated); the `FRAMEOS_QEMU_ACCEL` hook + this entry make them cheaply reconstructable.
  - **The one experiment NOT run (host was too saturated to trust):** a **load-controlled single-boot repro** — a kernel self-test doing N rename+read cycles within *one* boot, counting hangs/mismatches, so a fix can be A/B'd while host conditions are ~constant. That is the only methodology that could validate a guest fix here; worth building first if #110 is ever revisited as more than environmental.
- **2026-05-26 — #110 MITIGATED (interactive build) via a baked RAM disk — sidestep the host artifact instead of fighting it.** Since the trigger is QEMU's emulated-disk completion path (not the guest), the decisive move is to *not use that path* for the interactive workload. The `interactive` kernel now serves its filesystem from `kernel/src/ramdisk.rs`: the whole fs image is `include_bytes!(env!("FRAMEOS_BAKED_FS"))`-baked into the kernel (xtask assembles it + points the env var at it, before the kernel build; `build.rs` re-exports it via `rustc-env` and writes a zero placeholder for bare `cargo`/CI clippy so the feature always compiles), copied into a writable RAM buffer at boot, and every `read_sector`/`write_sector` is a plain `memcpy`. `fs.rs`'s `read_block`/`write_block` dispatch to the RAM disk under `#[cfg(feature = "interactive")]` and to virtio-blk otherwise.
  - **Result: deterministic.** On the *same saturated host* where the virtio-blk path was failing ~80%, the RAM-disk `console-test` ran **8/8 green** (and the single run that started this was clean through the full `mv`/`tcc` storm). The flake's mechanism — a lost/stale completion from the emulated device — simply doesn't exist when the data path is guest RAM.
  - **What's preserved:** the real virtio-blk driver + `BlockRequest`/`IoScheduler` Frame systems are *unchanged* and still built + validated by the non-interactive smoke suite (`blk_roundtrip_b4` etc.) and by real hardware — so the post/drain Frame demonstration is intact; only the *interactive* build swaps the backend. `mod virtio_blk` carries `#[cfg_attr(feature = "interactive", allow(dead_code))]` since its data path is unused there (its `on_irq` is still wired into the IDT).
  - **Honest scope:** the RAM disk is **ephemeral** — writes are RAM-only and reset to the pristine baked image each boot. This *matches* the existing workflow (every test run already starts from a fresh disk copy; nothing relied on cross-boot persistence) and is principled, not a cover-up: the guest was proven correct, so swapping the unreliable emulated device for a guest-owned one is the right call on this host. The durable path (virtio-blk → a real disk file) is unchanged and used on real targets. A **write-back** variant (serve from RAM, flush dirty blocks to the device at shutdown/sync) is the documented next step if persistence on the emulation host is ever wanted — deferred deliberately ("simplest first").
